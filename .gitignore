# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Local docker compose override (build-from-source for dev)
docker-compose.override.yml

# ZCode working files (test scripts, scratch DXF)
.zcode/

.mongo-data

.secret.json
.prod-secret.json
.**secret**.json


# Python
__pycache__
*.pyc
*.pyo
*.pyd
*.pyw
*.pyz
*.pywz
*.pyzw
**/venv/**
# test data 
.test-data

.mongo-data

.playwright-mcp/

# Rust build artifacts
workers/nesting/engine/target
workers/nesting/benchmarks/tmp
*.egg-info/

# LibreDWG tarball downloaded locally for checksum verification (the Docker build fetches its own).
libredwg.tar.xz

# Local product specs (decision memory) — intentionally NOT committed.
specs/

# Mission v2 — workspace géométrie Rust (workers/geometry)
workers/geometry/target
workers/geometry/parity/__pycache__
# dump de référence local (notes de reverse-engineering), jamais commité
workers/geometry/parity/SVGELEMENTS-REFERENCE.txt

# Scratch de replay du banc moteur (J-092)
workers/nesting/bench/out*/
docs/DEPLOY-HETZNER.md
workers/nesting/bench/out_*.svg
workers/nesting/bench/out_*.png
# Fixtures user : servent aux tests vitest (replayUserBpp, skipIf absent).
!workers/nesting/bench/out_user_*.json

# QA locaux (dumps bancs, captures, scripts jetables) — jamais commités
.qa-pw/
# spike : copie locale non suivie (wasm de dev, payloads)
spike/
