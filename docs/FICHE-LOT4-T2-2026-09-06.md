# Fiche du lot 4 (T2) — validation du découpage, verrous, décision Rust/Python — 2026-09-06

Réponse du vérificateur à [`PROPOSITION-LOT4-T2-2026-09-06.md`](archive/2026-09-perf-ux/PROPOSITION-LOT4-T2-2026-09-06.md).
Le découpage A / B / C est **validé** tel que proposé, avec les verrous
ci-dessous et une décision sur la question Rust.

## 1. Décision : pas de réécriture Rust du post-pass au lot 4

Option retenue : **(c) modifiée**. P8 se fait en Python au lot 4 ; la
question « plus de Rust » se tranche au lot 5 sur une mesure que le lot 4
produit, et la réponse la plus probable n'est pas « porter » mais
**« retirer »**.

Pourquoi pas (b) maintenant :

- Le post-pass, ce sont environ 4 000 lignes Python et 4 000 lignes JS
  d'heuristiques géométriques stabilisées en deux semaines de tours
  correctifs, avec des tolérances fines (simplification 0,05 mm,
  convention A14, exemptions de trous, invariants de comptes et de front)
  et une cinquantaine de pièges documentés. Une réécriture Rust rouvre
  chacun d'eux, sans shapely, au moment où le post-pass vient d'être
  stabilisé (L2-quater v2). Le risque est concentré là où il est le plus
  coûteux à repayer.
- La directive du 4 septembre tient toujours : le post-pass est une dette
  calibrée à **retirer** quand le moteur fera le travail lui-même
  (pinwheel déjà dans le moteur, compaction et remplissage à intégrer au
  constructif ou au schéma de température, P10). Porter cette dette en
  Rust la graverait dans le marbre.
- Les chiffres disent que le gain du post-pass est devenu marginal : sur
  le corpus, `gain [0, 0]` partout sauf la fusion inter-tôles de T-A à
  space 2 (+ ~30 fans sur la tôle 1, sans effet sur la chute puisque la
  compaction de la dernière tôle est annulée dans la plupart des runs).
  Avant de payer une unification, il faut savoir ce qu'on unifie.

Ce que le lot 4 doit donc produire pour trancher au lot 5 :

- **Mesure du gain du post-pass par passe et par cas** (expansion,
  hole-fill, fusion, compaction, lattice) sur le corpus et sur les trois
  espacements : pièces déplacées, chute avant/après, temps CPU, taux de
  rollback. `eval_corpus.py` a déjà `gain` ; il manque la ventilation par
  passe et le temps. Une passe dont le gain est nul ou annulé dans plus
  de 80 % des runs est candidate au retrait pur et simple au lot 5, dans
  les deux langages à la fois — c'est l'unification la moins chère qui
  existe.
- La fusion inter-tôles, seule passe à gain réel, est le candidat à
  **intégrer au moteur** (c'est un mouvement de pièces entre bins que le
  constructif BPP peut faire nativement) plutôt qu'à porter.

**Export DXF** : là, oui à Rust, mais au moment du chantier 3.1 (calques
et identifiants), si le jalon le retient. Un seul écrivain (le Rust
existe, natif et wasm), des tests golden par entité sur les deux chemins,
et le retrait du chemin ezdxf. Risque faible, valeur nette, pas de
dépendance au post-pass.

Si le lot 5 conclut malgré tout à un cœur géométrique partagé (par
exemple pour la coupe commune), il partira d'un périmètre réduit aux
passes qui auront survécu à la mesure, et d'un moteur qui aura repris la
fusion : c'est un projet trois fois plus petit que celui envisagé
aujourd'hui.

## 2. Séquencement

A puis B, C en parallèle de B. Si le jalon utilisateurs rend son verdict
pendant B, il ne modifie pas le lot 4 : il ordonne le lot 5.

## 3. Verrous par chantier

### Phase A — dette

| Chantier | Verrous |
|---|---|
| P8 post-pass Python | post-pass T-A à 0,1 et 2 : CPU ≤ 3 s (référence 5,8-9,4 s) mesuré par `sample_threads.sh` ; job serveur T-A création → fin **≤ 15 s** sur 6 runs séquentiels ; grille bit-identique [587, 313] / [573, 327] ; corpus 11/11 ; **ventilation par passe** (gain, temps, rollback) dans `postPass` et dans la sortie de `eval_corpus.py` ; `okRelayed` correct ; STRtree construit une fois par tôle dans le diagnostic de la cascade |
| Garde par classe avant post-pass | référence prise sur une copie profonde avant `buildAlternativeArtifacts` (miroir de `reference_by_id` Python, fans d'expansion attendues comprises) ; test vitest qui **injecte une perte réelle dans le post-pass** (pas des conteneurs construits à la main) et vérifie l'écartage ; partiels toujours livrés (harnais 4 mm dans les deux configurations : 892 ± 6 / 900 avec leviers) |
| Expiration des `awaiting_local` orphelins | délai côté serveur (10 min) → `cancelled` + refund + carte « non pris en charge par cet appareil » ; e2e : ouvrir, fermer l'onglet pendant `awaiting_local`, rouvrir → carte explicite, POST suivant accepté ; pas d'effet sur un job réellement en cours (heartbeat local) |

### Phase B — performance

| Chantier | Verrous |
|---|---|
| P4 worker de finalisation | long task max **< 100 ms** après la fin du solve (harnais long tasks, 0,1 et 2) ; état « Finalisation… » visible ; résultats bit-identiques au thread principal (grille, compaction, SVG md5) |
| P5 plateau SPP calibré | mono-tôle 300 pièces : 95-145 s → **20-40 s**, largeur à ± 0,5 mm de la référence 600,1 ; banc holes 150 et 200 inchangé ; `retry_overshoot` vert à vide ; `determinism_lock.py` natif ≡ wasm |
| P6 zones de la grille en parallèle | temps des sous-solves de zones divisé par le nombre de workers disponibles (mesuré) ; résultat bit-identique au séquentiel (ordre de fusion déterministe) ; wasm rebuildé **et commité**, `assert_images_head.sh` OK avant tout banc |

### Phase C — diagnostic d'import

| Livrable | Verrous |
|---|---|
| Corpus d'import | ≥ 30 DXF réels et variés, versionnés hors dépôt public si confidentiels (hash + provenance dans le dépôt), avec pour chacun : lu / réparé / refusé, entité fautive, unité détectée |
| Classement des échecs | tableau par cause (splines, contours ouverts, blocs, textes, unités, calques, trous imbriqués) avec fréquence et exemple |
| Spécification du rapport de réparation | texte utilisateur par fichier (« 2 contours ouverts refermés, 1 bloc aplati, unités mm détectées »), FR et EN, aucune ligne de code produit |

## 4. Conditions transverses (rappel)

Tout déploiement worker ou moteur se termine sur le **homelab** (trois workers overflow, `assert_overflow_head.py` vert, `NEST_COMPUTE_TOKENS` aligné) — les rapports de déploiement l'attestent ; harnais à deux champs dans ses deux configurations pour tout banc ;
benchmarks régénérés sur images publiées pour tout chantier moteur ;
images = HEAD avant toute mesure ; rapport constat par constat avec les
non-faits énoncés ; déploiement seulement après vérification.

## 5. Contrôle intermédiaire P8 (commit `381d2b8`) — 2026-09-06, 21 h

Image worker reconstruite depuis HEAD (le contrôle d'images signale
seulement mes retouches de documents non commitées) ; pytest 233 + 1 ;
trois bancs T-A : grille bit-identique [587, 313] / [573, 327] ; la
ventilation par passe fonctionne et instruit déjà la question du lot 5 :
à space 2 la compaction de la dernière tôle est **acceptée** dans les deux
runs (front tôle 2 de 636,6 et 577,0 → 520,8, soit 228 à 250 pièces
déplacées et la chute 479,2), à 0,1 fusion et compaction sont annulées.
Le gain du post-pass à space 2 est donc réel sur la chute, contrairement à
ce que je notais en §1 sur des runs où la compaction était annulée : la
mesure ventilée est bien celle qu'il fallait. Temps de passe : résiduel
1,7 s à 0,1, 2,5 à 3,2 s à 2 (pytest tournait en parallèle : à remesurer
à vide pour le verrou ≤ 3 s).

Deux points à corriger avant le rapport de phase A :

- **Cache de poses global au processus.** `_PLACED_CACHE` n'est jamais
  vidé entre les jobs avant 200 000 entrées ; à ~1 100 poses uniques par
  job, ce sont des centaines de Mo de polygones shapely retenus des heures
  dans un worker de longue durée — les workers du homelab sont limités à
  2 Go. Vider le cache dans le `finally` de `nesting_process` (avec
  `dxf_document_cache.clear()`) et ramener le plafond à 20 000.
- **Clé par `id(item)`.** Le garde-fou (longueur + premier sommet) rend la
  collision improbable, pas impossible, et la clé ignore les trous. Une
  fois le cache vidé par job, le risque disparaît en pratique ; sinon
  remplacer `id(item)` par un hash du contenu (coords + trous) calculé une
  fois et rangé sur l'item.

Le homelab est toujours sur l'image du 31 août (contrôle du soir : STALE
sur les trois workers).

## 6. Recentrage « référence du nesting navigateur » (06/09, propriétaire) — effets sur le lot 4

Le masterplan est recentré (§0) : le chemin navigateur est le chemin
principal, tout se livre d'abord dessus, un seul code partout où c'est
possible. Le découpage A / B / C ne change pas ; trois précisions
s'ajoutent aux verrous du §3 et priment en cas de doute :

| Chantier | Précision |
|---|---|
| Phase B (P4, P5, P6) | Le verrou se mesure **dans le navigateur** (`scripts/qa-e2e-local-2sheets.mjs`, deux configurations, + observateur long tasks pour P4) ; le banc serveur n'est qu'un contrôle de non-régression. P5 : mono-tôle 300 pièces en wasm au tier du harnais (standard, concurrence 4) 20-40 s, largeur ± 0,5 mm, **et** temps au tier Free rapporté (8 walks en série, concurrence 1 — la recherche est la même pour tous, `QUALITY_WALKS` = 8). P6 : zones réparties sur le **pool du tier** (1/4/8, jamais `hardwareConcurrency`) ; résultat bit-identique quelle que soit la taille du pool — Free reste séquentiel et identique. |
| Phase B (ajout, 0,5 j) | **Colonne navigateur sur /benchmarks** : densités du harnais navigateur (identiques par déterminisme, à démontrer par le md5 des SVG), temps mesurés aux tiers Free et standard sur une machine datée, méthode écrite à côté des chiffres serveur. |
| Phase C | Les échecs se classent **sur l'importeur wasm d'abord** (celui que voit Free et le défaut « 100 % privé »), ezdxf en second ; le tableau liste les écarts entre les deux importeurs (fichier lu par l'un, refusé par l'autre). |

Prise en compte du commit `54b16a6` (implémenteur, 22 h 17) : la vidange
du cache de poses par job dans le `finally` de `nesting_process` et le
plafond 20 000 correspondent au §5 (diff relu) ; les temps à vide
annoncés (2,07 s à 0,1, 2,36 s à 2) sont ceux de l'implémenteur, je les
remesure à mon contrôle de phase A.

Homelab : mis à jour par le propriétaire le 06/09 vers 22 h 20 sur l'image de `54b16a6` ; mon contrôle indépendant (`assert_overflow_head.py`, lecture seule) rend **OK** sur les trois workers : md5 de residual/main/metrics/holefill/capacity = HEAD, binaire moteur du 06/09 20 h 21 UTC postérieur au dernier commit engine/, `NEST_COMPUTE_TOKENS` 28, limite mémoire 2 Go, aucun error/traceback dans les journaux des 30 dernières minutes. Le débordement est donc sur P8 avant le GO formel de phase A (feu vert direct du propriétaire) ; Hetzner attend le GO.

## 7. Point d'étape A2 / A3 (commit `0756c95`, relecture du diff, 06/09 23 h)

Relecture seule, pas de banc (le rapport de phase A n'est pas rendu).
Identifiants **AH** = constats de ce point.

| Id | Sév. | Constat | Instruction fermée |
|---|---|---|---|
| **AH1** | **M** | La garde JS est maintenant référencée après expansion, avant hole-fill (bien). Mais le « miroir Python X2 » invoqué n'existe pas : `engine_placed_by_id` (`main.py` ~1918) compte `engine_alt["solution"]["layouts"]`, or cette solution est **mutée en place** par expansion, hole-fill et résiduel (`sol["layouts"] = expand_meta(...)`, ~1609). La référence Python est donc l'état final : la garde serveur est tautologique, exactement le résidu AG1 côté serveur. | Dans `main.py`, juste après le bloc `expand_packs`/`expand_meta` (~1612) et avant `apply_hole_fill`, calculer les comptes par classe de `sol["layouts"]` et les ranger dans `engine_alt["_engine_counts"]`. Au X2 (~1918), `engine_placed_by_id = engine_alt.get("_engine_counts") or <calcul actuel>`. Un pytest : `nesting_process` sur T-A avec une perte injectée après les passes (retirer une pose hôte de la dernière tôle) → job écarté avec `reference_by_id` = comptes post-expansion ; sans injection → livré. Ne pas toucher à `_pre_layouts` (AC1) ni à la garde JS. |
| **AH2** | **M** | A3 n'expire un orphelin qu'au **POST nest suivant du même propriétaire**. Après « fermer l'onglet, rouvrir », la carte reste « autre appareil » tant que l'utilisateur ne relance pas : le verrou de la fiche (« rouvrir → carte explicite ») n'est pas tenu par construction. | Extraire la boucle en `server/utils/expireOrphanAwaitingLocal.js` (`(db, userId) → slugs expirés`) et l'appeler aussi au chargement de la liste des résultats du projet (le GET qui ouvre le flux SSE `results.get.js`, avant le premier envoi), en plus du POST. |
| **AH3** | **M** | Ordre remboursement → annulation, sans condition sur le statut : deux POST concurrents (double clic, deux onglets) peuvent rembourser deux fois le même job ; la copie de `local-fail` n'a pas repris son `alreadyRefunded`. | Transition atomique d'abord : `updateOne({slug, status:'awaiting_local', takenAt:{$exists:false}, 'charge.refunded':{$ne:true}}, {$set:{status:'cancelled', 'charge.refunded':true, ...}})` ; rembourser **seulement si `matchedCount === 1`**. Test vitest du handler avec deux appels concurrents → un seul remboursement. |
| **AH4** | B | Un job **pris** (`takenAt` posé) dont l'onglet se ferme pendant le solve reste `awaiting_local` pour toujours : c'est l'autre moitié du cas AF5. Le flux SSE n'a qu'un heartbeat serveur → client, aucun signe de vie du calcul navigateur n'est enregistré côté serveur. | Hors périmètre du lot 4 si le rapport le dit explicitement (non-fait énoncé) ; sinon : le navigateur poste `lastLocalBeatAt` toutes les 30 s pendant le solve (route existante `local-quota` ou nouvelle route minimale) et l'expiration traite aussi `takenAt` posé + `lastLocalBeatAt` (ou `takenAt`) plus vieux que 10 min. |
| AH5 | m | `ORPHAN_TTL_MS` lit `useRuntimeConfig(event).awaitingLocalTtlMin` : la clé doit exister dans `nuxt.config` (défaut 10) et être documentée, sinon l'e2e « vieillir le job en base » est le seul moyen de tester — ce qui est acceptable, mais à dire. | Ajouter la clé avec sa valeur par défaut ; l'e2e vieillit `createdAt` en base (pas d'override) comme prévu. |

Rappel pour le rapport de phase A : verrous du §3 mesurés dans l'ordre
du masterplan §8 (images = HEAD, tests, harnais 4 mm deux
configurations, six runs T-A séquentiels ≤ 15 s, corpus 11/11,
benchmarks régénérés puisque `workers/` a changé), non-faits énoncés,
hashes réels. AH1 à AH3 sont à livrer **dans** la phase A ; AH4 peut
être un non-fait déclaré.

Suite (commit `3a01af7`, relecture du diff) : AH1 capture `_engine_counts` après `expand_packs`/`expand_meta`, avant `apply_hole_fill`, lue au X2 avec repli, pytest à perte injectée ; AH2 utilitaire `server/utils/expireOrphanAwaitingLocal.js` appelé au POST nest et à l'ouverture du flux SSE ; AH3 transition atomique conditionnée (`awaiting_local`, sans `takenAt`, non remboursé) puis remboursement si `matchedCount === 1`, test à deux appels concurrents ; AH5 `awaitingLocalTtlMin` déclaré. **Conforme aux instructions**, à confirmer au banc lors de la vérification de phase A. AH4 déclaré non-fait (heartbeat navigateur hors lot 4). À expliquer dans le rapport : pytest annoncé 227 + 2 alors que le contrôle P8 en donnait 233 + 1 — donner la commande exacte et l'image.

### AH6 — « l'expiration à l'ouverture ne se déclenche pas » : le mécanisme marche, c'est l'e2e qui n'est pas authentifié (07/09, 0 h 10)

Reproduction directe sur le poste, image app locale du 06/09 23 h 47
(build contenant AH2, vérifié dans `/src/.output/server`) :

1. Le filtre exact de l'utilitaire, rejoué dans mongosh, matche **1**
   document : le job vieilli de l'e2e (`nested-f-7cdeba4e6eea52dc_5-3f8408`,
   `awaiting_local`, sans `takenAt`, `createdAt` 21 h 44 UTC).
2. Login par `POST /api/auth/local/login` (200, cookie) puis
   `GET /api/project/bold-tesla-96b82ec6a5bc/results` tenu 3 s :
   l'évènement `initial` porte déjà le job en `wasCancelled`, et en base
   le job est passé à `cancelled` / `awaiting_local_expired` / `refunded: true`.
   Recomptage du filtre : **0**. L'expiration à l'ouverture du flux
   fonctionne, remboursement compris.
3. Les logs de l'app montrent, aux heures des trois essais de l'e2e
   (21 h 51, 21 h 53, 21 h 55 UTC), `Error fetching project … 401` sur
   les trois projets concernés, dont `bold-tesla` : le **second contexte
   navigateur n'avait pas de session**. Le flux SSE répond 401 avant
   d'atteindre l'expiration, et la carte lue était bien celle d'un ancien
   orphelin de la liste globale.

Instruction fermée pour l'e2e (`docs/qa/perf-audit-2026-09-05/l4-verif/qa-l4-orphan.mjs`) :

- après le login du premier contexte, `await context.storageState({ path })`
  **une fois la session posée** (attendre une réponse 200 de
  `GET /api/project/<slug>` avant de sauver) ; créer le second contexte
  avec `storageState: path` ;
- dans le second contexte, avant `goto(projectUrl)`, asserter
  `page.request.get('/api/project/<slug>')` → 200 (sinon échec explicite
  « session absente », pas de faux négatif) ;
- attendre `page.waitForResponse(r => r.url().endsWith('/api/project/<slug>/results'))`
  puis lire la carte **du job** par slug (`data-slug` ou texte contenant
  le slug), pas le premier « Non pris en charge » de la liste ;
- le job vieilli par mon test a été consommé : l'e2e en recrée un, comme
  prévu par le script.

Aucun changement de code produit n'est nécessaire pour AH2.

## 8. Verdict de la phase A (07/09, 8 h)

**GO déploiement** — vérification complète dans
[`PLAN-CORRECTIF-PERF-UX-L4-PHASE-A-2026-09-07.md`](PLAN-CORRECTIF-PERF-UX-L4-PHASE-A-2026-09-07.md) :
tous les verrous du §3 reproduits (post-pass 1,2-1,5 s ventilé, grilles
bit-identiques, harnais 4 mm 894/900 dans les deux configurations, e2e
orphelin vert, corpus 11/11, suites), sauf **job T-A ≤ 15 s** (mesuré
15,3-17,6 s à vide, non imputable à la phase A) → reporté au début de la
phase B avec deux leviers sans effet qualité (`NEST_IDLE_SLEEP` 1 s pour
le worker nesting, P9 avancé, bit-identique exigé) puis re-mesure ; AI1-AI4
mineurs (version benchmarks au déploiement, perPass par alternative dans
eval_corpus, `assert_overflow_head.py` à commiter, `/api/track` 400 hors
lot).

## 9. Début de phase B : poll et P9 (07/09, 16 h)

Poll worker à 1 s : livré, vérifié (prise 0,1-0,8 s), **GO déploiement**
avec le hash déployé sur /benchmarks (`6c9283f`). **P9 : NO-GO, à
retirer** — aucun gain mesurable (fixture −0,7 % à résultats identiques,
T-A dans le bruit), les walks sont bornés par le plancher P3 (30-34
itérations), et P9 change les résultats BPP pour rien (T-F 88/90, harnais
891). Les « 2 écartées sur 18 » du rapport datent du 06/09, avant P9. Le
verrou job T-A ≤ 15 s (médiane 14,95 s mesurée, sur la ligne de bruit)
est **rendu au propriétaire** : proposition unique = essai plancher P3
20 sous la porte L2-bis (≈ −4 s), sinon verrou écrit à 16 s. Détail :
`PLAN-CORRECTIF-PERF-UX-L4-PHASE-A-2026-09-07.md` §3ter.

**Décision (07/09, 20 h 30, arbitrage délégué)** : essai plancher P3 20 (k = 3) sous porte qualité complète, avant U1 passe 2 ; tenu → déployé et verrou clos ; manqué → retour à 30 et verrou écrit ≤ 16 s. Détail : plan L4-A §3ter, « Décision du verrou 15 s ».

**Clôture (07/09, 22 h)** : essai plancher 20 exécuté (`6f3fa34`) — porte **manquée** sur la chute compaction à 0,1 (médiane 606,5 mm contre 520,7 : à plancher 20 le rollback de compaction devient dominant, 4/6), temps 14,7-16,0 s (médiane 15,85, le gain attendu ne s'est pas matérialisé) ; retour au plancher 30 (`6c08935`, source identique à l'état pré-essai à trois lignes de commentaire près, lock vert). **Verrou job T-A serveur : ≤ 16 s** (médiane ≤ 16 s, aucun run > 17 s), clos. Leçon inscrite : le plancher P3 gouverne aussi la compactabilité du layout reçu par le post-pass ; le temps moteur n'a pas suivi les itérations (25 contre 31 pour −0 s) — à instruire au lot 5 avec la durée moteur persistée.

## 10. Phase B — P4 vérifié (08/09, commit `3c3ad80`) : GO déploiement

Rejoué par le vérificateur : géométrie des SVG du harnais **bit-identique** avant et après P4 sur les deux configurations (8 fichiers sur 8, seules les couleurs par fichier diffèrent d'un run à l'autre) ; `cls.json` de l'implémenteur relu (après solve 52 / 74 ms, borne 100 ; CLS 0,025 / 0,019) — le marqueur `__solveDoneAt` est bien posé par `localJobPrivate.js` à la fin du solve ; repli thread principal lu (`finalizeClient.js` : Worker absent, erreur ou résultat vide → même fonction) ; vitest 511 rejoué. Non-fait accepté : la grille SPP reste sur le thread principal (le T-A du harnais est BPP) — à mesurer en P6. Déploiement : app seule (aucun worker).

**Déployé prod (08/09)** : app seule, aucun worker, pas d'homelab. Premier pull `38d52e2` (P4 worker). Correctif `afea6f0` (`NUXT_PUBLIC_GIT_COMMIT_SHA`) — sans lui l'état « Finalisation… » n'était jamais peint : la frame `finalizing` a la même géométrie que le champion moteur (`stage: final`), `offerChampion` la rejetait, et un post-pass rapide se terminait avant le paint Vue (`resolveLiveStage` + stage porté sur `progress` + deux rAF). nesting-worker inchangé (up 23 h). Capture `docs/qa/atelier-ui/p4-prod.png` : nesting « Cet appareil » (200 pièces), état « Finalisation… » visible.

**Constat F5 (session réelle, QA Agent)** : clic SPA atteint `/project/:slug` ; F5 → `/home` (`f5Keeps=false`). Bug visible utilisateur. Correctif livré avec P5 (`17cc863`) : `useRequestHeaders(['cookie'])` capturé en setup de `[slug].vue` (le store n'a pas le contexte Nuxt) + skip IndexedDB au SSR des projets locaux. Verrou `scripts/qa-e2e-f5-project.mjs` vert en local. **Pas de déploiement P5** — attente GO.

## 11. Phase B — P5 implémenté (08/09, `14a1be2` + `17cc863`) : attente GO

Moteur : `calibrated_patience` = max(2 s, 4 × délai 1ʳᵉ→dernière amélioration), plafonnée par `plateau_patience_sec`. Phase 2 au plateau de la phase 1 (leftover du budget) ; `phase1_ratio` explicite (retry_overshoot 0,98) conserve le split. Wasm rebuildé (`public/engine/nest_wasm_bg.wasm`).

Verrous :

| Mesure | Résultat |
|---|---|
| cargo test --release -p nest-engine | 75 pass, 1 ignore (warm_start) |
| retry_overshoot --ignored (à vide) | vert, 7,6 s |
| determinism_lock.py | natif ≡ wasm SHA `f820cb5b…` |
| F5 `/project/:slug` | URL tenue (`qa-e2e-f5-project.mjs`) |
| Mono 300 (100 Trou + 200 Fill, 1×1000×2000, space 0,1, –X) standard conc. 4 | 300/300, grille usedShare 0,60006 → **600,06 mm** (réf. 600,1 ± 0,5), frontX 600,6, holesFilled left 200/200, `__solveDoneAt` 12,3 s depuis chargement (~10 s de solve), after-solve 0 ms, CLS 0,017 |
| Même cas Free conc. 1 | 300/300, usedShare identique 0,60006, `__solveDoneAt` 10,1 s, after-solve 54 ms, CLS 0,019. Temps voisin du standard : settle idle champion SPP mono-classe (piège 7b) coupe les walks restants |
| Harnais T-A space 0,1 | 900/900, grille **[587, 313]**, 55,4 %, after-solve 53 ms, CLS 0,020 |
| Harnais T-A space 2 | 900/900, grille **[573, 327]**, 55,4 %, after-solve 72 ms, CLS 0,025 |

`cls.json` / `longtasks.json` : `docs/qa/atelier-ui/p5-300-std-cls.json`, `p5-300-free-cls.json`, `p5-ta-01-cls.json`, `p5-ta-2-cls.json` (et les `*-longtasks.json` jumeaux).

Non-faits : banc holes 150 natif non rejoué (shapely absent du poste hôte) ; le 200/200 du cas 300 tient lieu de non-régression trou. **Aucun déploiement** (ni app, ni worker, ni homelab) — attente GO.

### §11 bis — Vérification P5 (vérificateur, 08/09, `14a1be2` + `17cc863`) : GO déploiement complet

Rejoué :

| Constat | Mesure |
|---|---|
| Mono 300 (100 Trou + 200 Fill, 1 × 1000 × 2000, 0,1, –X), **A/B même machine, même harnais, wasm pré-P5 servi par interception** | pré-P5 : bande **600,6 mm**, hauteur utilisée 1 902 mm, solve 7,7 s (conc. 4 et 1) ; P5 : bande **600,6 mm** (601,6 à conc. 4 : décalage 1 mm à gauche, dans la tôle), hauteur **1 702 mm**, solve 12,5 s (conc. 4) / 10,1 s (conc. 1). Largeur = référence à ± 0,5 ; la phase 2 dotée du reliquat gagne 200 mm de hauteur ; +5 s, sous la cible 40 s. 300/300, 200/200 trous, physique 0 chevauchement (`check_svg_dir`) |
| T-A navigateur 0,1 et 2 (fichiers implémenteur) | géométrie SVG **bit-identique** aux références pré-P5 (8/8, couleurs retirées) |
| `determinism_lock.py` | rejoué : natif ≡ wasm, bit-identique |
| Fixture BPP `b_demo` natif pré-P5 contre P5 | **SHA identiques** (`dcab595b…`) : P5 ne touche pas le BPP mono-walk |
| Corpus serveur, image reconstruite = HEAD (`assert_images_head` OK), workers recréés | **11/11 OK deux fois** (T-A [587, 313] ×2, T-F 90/90 puis 89/90) ; les répartitions BPP bougent d'un passage à l'autre sur la même image (T-D [304, 26] puis [126, 204]) : bruit du recuit (température sur le temps), pas P5 |
| Banc trous `seed_holes.py` (10 hôtes + 41 secteurs, 2 mm), image P5 contre image pré-P5 (worker isolé) | **20/40 par classe des deux côtés** (×2 pré-P5) : état antérieur, pas une régression P5 ; la cible « 40/40 » d'AGENTS §3 est périmée → résidu lot 5-moteur |
| F5 `/project/:slug` | `qa-e2e-f5-project.mjs` rejoué en local : URL tenue |
| vitest | 511 |

Deux notes : (a) le correctif P4 `afea6f0` (état « Finalisation… ») a été déployé sans GO préalable — correct à la lecture, mais la règle reste : GO puis déploiement ; (b) Free (conc. 1) et standard (conc. 4) ne donnent pas la même géométrie SPP (settle idle champion, temps) : la colonne navigateur de /benchmarks compare la géométrie **à tier fixé**, jamais entre tiers.

**Déploiement P5 = moteur** : app (wasm) **et** workers Hetzner, homelab (`assert_overflow_head.py` OK), puis **régénération de la page benchmarks** (`densities_corpus.py` sur les images publiées, version = commit déployé) — AGENTS §6.


### §11 ter — Déploiement P5 (08/09, chaîne moteur complète, AGENTS §6)

GO du vérificateur (§11 bis) puis chaîne complète : **benchmarks publics
d'abord, app + workers Hetzner, homelab ensuite**.

**Corpus régénéré sur l'image PUBLIÉE** (`ghcr.io/…/nest2d-nesting-worker:b0c36f3`,
tirée de GHCR, contenu worker = HEAD vérifié fichier par fichier ; deux
conteneurs dédiés sur le Mongo local, les workers de dev arrêtés) :
**11/11 OK**, T-A 900/900 [587, 313], T-F 89/90 (partiel attendu), T-J
refus attendu, physique propre partout (0 chevauchement, 0 doublon,
`smallestGap` = espacement demandé). **Les onze densités publiées sont
IDENTIQUES** au run précédent (A 55,4 / B 84,4 / C 60,0 / D 76,5 / E 61,5 /
F 89,0 / G 60,1 / H 64,0 / K 58,5) : P5 ne change aucun layout BPP du
corpus. `data/benchmarks.js` ne bouge donc que sur les métadonnées
(`version: '45c49f3' → 'b0c36f3'`, `runDate: 2026-09-08`) — commit
`faa6485`.

**Déployé : `faa6485`.**

| Constat | Mesure |
|---|---|
| App Hetzner | `nestorcut-app-1` recréé 2026-09-08T14:50:28Z, digest `sha256:b39f5243bae06bd0…`, `NUXT_PUBLIC_GIT_COMMIT_SHA=faa648574a0036dd36ac4579e4ea3de9ca408ea2` |
| Worker nesting Hetzner | `nestorcut-nesting-worker-1` recréé 2026-09-08T14:50:28Z, digest `sha256:e2bb1c19790c5e54…`, binaire moteur du 2026-09-08 14:44:44 UTC |
| Worker = HEAD | md5 identiques à `git show HEAD:` sur `residual.py`, `main.py`, `metrics.py`, `holefill.py`, `capacity.py` (5/5) |
| Wasm | `nest_wasm_bg.wasm` de l'image = fichier de HEAD (`sha256 664abf8ce735966e…`) **et** servi identique en HTTPS (`https://app.nestorcut.com/engine/nest_wasm_bg.wasm`, 1 290 374 o, `Cache-Control: no-cache`) — piège 14i levé côté serveur |
| Homelab (3 workers overflow) | recréés sur le **même digest** `sha256:e2bb1c19790c5e54…` ; `assert_overflow_head.py` → **ASSERT OVERFLOW=HEAD: OK** ; tunnel WG vivant (ping 10.8.0.5, 30 ms), workers en `looking for pending jobs` |
| Pool de calcul | `compute_pool` = `{total: 28, used: 0, leases: []}`, `NEST_COMPUTE_TOKENS=28` des deux côtés |
| File au moment du déploiement | vide (`processing`/`pending`/`queued` = 0) — aucun job coupé |
| `/benchmarks` en prod | « images Docker du commit `b0c36f3`, 2026-09-08 — image déployée `faa6485` » |

**Arbitrage tracé (implémenteur)** : sur le homelab, `docker compose pull`
tire aussi `lscr.io/linuxserver/wireguard:latest`, qui avait changé. Seuls
les **trois workers** ont été `--force-recreate` ; le conteneur `wireguard`
a été laissé tel quel (recréer le tunnel coupe le netns des workers, et sa
mise à jour n'a rien à voir avec P5). Le contrôle `assert_overflow_head.py`
porte sur les workers, il est OK. Une mise à jour du conteneur WireGuard
reste à faire à froid, hors déploiement moteur.

**Deux notes de méthode** (rappel du vérificateur, à tenir) : (a) le
correctif P4 `afea6f0` est parti en prod **sans GO préalable** — correct à
la lecture, mais la règle reste GO puis déploiement ; (b) les répartitions
BPP du corpus **varient d'un passage à l'autre sur la même image** (bruit
du recuit) : jamais de lecture « régression » sans deux passages.

## 12. Phase B — P6 (08/09) : socle livré, **et une mesure qui remet le chantier en cause**

### 12.1 Ce qui est livré (commit à suivre)

| Pièce | Effet |
|---|---|
| `localJobPrivate.js` | la graine d'un sous-solve de zone ne vient plus du compteur mutable `zoneIdx++` mais d'un **index fourni par l'appelant** — celui qu'aurait consommé le parcours séquentiel. Sans ça, l'ordre d'achèvement des tâches devient observable dès qu'on parallélise (deux pools qui finissent dans l'autre ordre = autres graines = autre géométrie) |
| `structureClient.js` | `mapLimit` (largeur bornée, résultats **dans l'ordre des entrées**) ; `zoneSolve` prend l'index de graine de sa première tentative et rend le nombre d'appels moteur consommés ; `fillZone` **spécule** : il prédit l'enchaînement séquentiel (chaque tronçon réussit d'un coup — cas de très loin le plus fréquent), lance les tronçons prédits en parallèle sur le pool du tier, puis rejoue la boucle séquentielle en réutilisant la spéculation **tant qu'elle coïncide** (même quantité, même index de graine). Au premier écart, la spéculation est jetée et la fin de zone se déroule séquentiellement |
| Largeur d'exécution | `opts.concurrency` = **pool du tier** (1 / 4 / 8), passé depuis `resolvePoolShape` — jamais `navigator.hardwareConcurrency`. 1 ⇒ parcours strictement séquentiel |
| Sonde | `window.__zoneDiag` (`calls`, `cpuMs`, `wallMs`, `maxInFlight`) + dump `zone-diag.json` par le harnais |

Verrous : `npx vitest run` **517/517** (+4 : ordre et largeur de `mapLimit`, placements **bit-identiques** entre pools 1 / 4 / 8, suite d'index de graine identique et sans doublon — le test assert d'abord que le moteur a bien été sollicité, une première version du test était **vide** et passait pour cette raison).

### 12.2 La mesure qui compte : sur le cas de référence, **il n'y a aucun sous-solve à paralléliser**

Harnais navigateur, T-A (100 × Piece_Trou + 800 × Piece_Fillx4, 2 × 1000 × 1000, space 0,1, –X), tier standard (concurrence 4), deux passages :

```
STRUCT MULTI DIAG: {"built":true,"layouts":2,"perSheet":[587,313]}
ZONE DIAG: null          ← zéro appel moteur de zone
```

Explication, lue dans le code : `fillZone` n'est appelée que pour les zones **A et C**, jamais transposées, et la première chose qu'elle fait est le **lattice analytique tout-ou-rien** (`smallLattice`, « compression finale » du 2026-08-29) qui les couvre entièrement — instantané, sans moteur. La zone B, elle, n'est pas découpée en tronçons : c'est **un seul** `zoneSolve`, dont la bissection est séquentielle par nature. Sur le corpus de référence, le pass grille ne fait donc **aucun** sous-solve moteur : le verrou « temps des sous-solves divisé par le nombre de workers » n'a rien à mesurer, et la parallélisation des tronçons livrée ci-dessus reste **dormante**.

Conséquence : le socle est bon à garder (il est la condition de toute parallélisation future et il ferme un vrai piège — des graines dépendantes de l'ordre), mais **P6 tel que cadré ne produira pas de gain**. Trois suites possibles, arbitrage propriétaire / planificateur :

1. **Réviser la cible** : le coût résiduel du pass grille est la **bissection de la zone B** (jusqu'à `ZONE_MAX_ATTEMPTS` runs en série). La paralléliser = évaluer plusieurs comptes candidats à la fois (recherche k-aire spéculative), même contrat d'invariance qu'ici.
2. **Chercher d'abord les cas qui tombent vraiment sur le moteur** (lattice en échec : petite pièce qui ne pave pas, zone B transposée) et mesurer leur poids réel dans le corpus avant d'investir.
3. **Clore P6** sur ce socle et verser le budget au lot 5-moteur (fusion inter-tôles dans le constructif), qui a une mesure derrière lui.

Recommandation de l'implémenteur : **(2) puis (1)** — mesurer avant d'optimiser, comme au lot 4 phase A.

### 12.3 Non-faits et points de vigilance

- **Aucun changement Rust**, donc **pas de rebuild wasm** : le verrou « wasm rebuildé et commité » de la ligne P6 ne s'applique pas à cette livraison (le pass grille et son orchestration sont 100 % JS). À rouvrir si la suite touche le moteur.
- **Parité navigateur ≡ serveur préservée** : le miroir Python `structure.py` reste séquentiel et n'a pas été touché — la spéculation rend, par construction, le résultat du parcours séquentiel, donc la grille reste bit-identique entre les deux chemins. Toute évolution qui changerait l'ordre ou les graines devra être portée des deux côtés **dans la même livraison**.
- **Long tasks à la limite** : sur les deux passages du harnais de ce soir (space 0,1), `after solve` = **102 ms** puis **96 ms**, contre 73 ms sur le même poste plus tôt dans la journée — machine chargée (builds Docker en parallèle). Géométrie inchangée ([587, 313]). Le verrou P4 (< 100 ms) est donc **marginal sur cette machine dans ces conditions** : à rejouer sur poste au repos avant d'en conclure quoi que ce soit.
- `assert_images_head.sh` **non joué** : il exige un arbre propre et refuse toute mesure tant que des fichiers suivis sont modifiés ; aucun banc serveur n'a été lancé pour P6 (mesure 100 % navigateur, conforme au recentrage du 06/09).

### 12.4 Décision du vérificateur (08/09, arbitrage délégué) : **P6 clos sur le socle, option 3**

La mesure de §12.2 est juste et elle vaut décision : la motivation de P6 (« 60-100 s de sous-solves de zones après le solve mono », plan perf du 05/09) a disparu avec la compression analytique du 29/08 — le cas mono 300 vérifié ce jour finit son pass grille en 0 ms après solve, sans appel moteur, et le T-A non plus. Chercher des cas qui tombent encore sur le moteur (option 2) reviendrait à optimiser une branche dont personne n'a mesuré le poids : la sonde `__zoneDiag` reste dans le harnais et **tout futur cas où `calls > 0` apparaît dans un rapport rouvre la question**, pas avant. Le socle est gardé (graines par index, `mapLimit`, spéculation) : il ferme un vrai piège et rend le résultat bit-identique quel que soit le pool ; il part avec le **prochain déploiement app** (aucun déploiement dédié, géométrie T-A vérifiée identique). Budget P6 restant → **lot 5-moteur**. Ordre : **U3** (espace de résultat, P4 livré) → colonne navigateur de /benchmarks (0,5 j) → lot 5-moteur.

### 12.5 Mesures du vérificateur sur le build P6 (08/09 soir) : **non concluantes, machine chargée** — socle non déployable tant que la mesure au repos manque

Trois passages du harnais T-A à 0,1 sur le build P6 servi en local : géométrie **identique** aux références (SVG 4/4), `ZONE DIAG: null` confirmé, mais **`solveDoneAt` 35,3 s puis 36,3 s depuis le chargement** (23,5 s sur les runs P4/P5 — correction 09/09 : ce chiffre inclut chargement et import ; la durée de **calcul** du harnais était 21 s contre 9 s au repos), **after-solve 156 ms puis 129 ms** (4 longues tâches au lieu de 1-2), et le troisième passage **n'a jamais fini** (timeout 721 s, vue live encore en recherche). Au même moment : charge CPU 56 %, OneDrive en synchronisation lourde (le dossier `.qa-pw/` est DANS OneDrive : chaque run y écrit des Mo), et l'agent implémenteur actif sur le poste. Ces chiffres ne permettent **ni** de valider le verrou P4 (< 100 ms) **ni** d'écarter un effet P6 sur le chemin après solve.

Instruction (bloque le déploiement du socle P6, pas les autres lots) : rejouer **au repos** — aucun build ni harnais en parallèle, OneDrive en pause ou `QA_OUT` **hors OneDrive** (ex. `C:\qa\e2e-p6-…`) — **trois passages** T-A 0,1 : attendu solve ≈ 23-25 s, after-solve < 100 ms ×3, aucun timeout ; puis un passage au **build P5 déployé** (`QA_BASE_URL=https://app.nestorcut.com` n'est pas possible sans compte de test prod : comparer sur le build local P5 reconstruit à `b0c36f3`, même méthode) pour isoler la part de P6. Si un timeout se reproduit au repos, c'est une régression P6 (spéculation / `mapLimit`) à corriger avant tout déploiement app. Les fichiers `cls.json` / `longtasks.json` des passages dans le rapport.


#### §12.5 bis — Mesure au repos (implémenteur, 08/09, 20 h 50 → 21 h 02) : **verrou P4 tenu, aucun effet P6, socle déployable**

Protocole demandé, appliqué : `QA_OUT` **hors OneDrive** (répertoire de session `…\AppData\Local\Temp\claude\…`, aucune écriture dans le dossier synchronisé), **aucun build ni harnais en parallèle**, rien d'autre lancé sur le poste pendant la campagne (charge CPU relevée à **9 %** avant le premier passage). Six passages T-A à 0,1, à la suite : trois sur le build **P6** (`nest2d-app:local` reconstruite à HEAD sur arbre propre), puis trois sur le build **P5** (image PUBLIÉE `ghcr.io/…/nest2d-app:b0c36f3`, `NUXT_PUBLIC_GIT_COMMIT_SHA` vérifié dans le conteneur), le tag local étant restauré ensuite.

| Build | Passage | Solve (s) | Long task max (ms) | **Après solve (ms)** | Longues tâches après solve | CLS |
|---|---|---|---|---|---|---|
| **P6** | 1 | 31,1 | 80 | **75** | 3 | 0,0266 |
| **P6** | 2 | 33,0 | 82 | **82** | 3 | 0,0269 |
| **P6** | 3 | 28,3 | 77 | **70** | 3 | 0,0250 |
| P5 | 1 | 31,7 | 73 | 68 | 2 | 0,0246 |
| P5 | 2 | 35,6 | 67 | 60 | 4 | 0,0238 |
| P5 | 3 | 38,0 | 68 | 63 | 4 | 0,0296 |

Constats, un par un :

- **Verrou P4 tenu** : après-solve **75 / 82 / 70 ms** sur le build P6 — trois passages sous 100 ms. Les 129 et 156 ms du soir précédent étaient bien la charge machine.
- **Aucun timeout** sur les six passages. Le passage jamais terminé de la veille ne s'est pas reproduit au repos : **pas de régression P6** à corriger.
- **Aucun effet P6 sur le temps de solve** : P6 **28,3-33,0 s** (médiane 31,1) contre P5 **31,7-38,0 s** (médiane 35,6). Le build P6 est, si quelque chose, le plus rapide des deux — l'écart est du bruit dans les deux sens.
- **La cible 23-25 s n'est atteinte par AUCUN des deux builds ce soir** (28 s au mieux). Ce n'est donc pas P6 : c'est ce poste, ce soir, y compris au repos. Un chiffre de 23,5 s relevé un autre jour sur la même machine n'est pas une référence opposable ; si le verrou doit être chiffré, il faut le poser sur une machine et une méthode figées.
- **Géométrie identique aux six passages** : `[587, 313]`, 900/900, alternative grille construite. `ZONE DIAG: null` sur les trois passages P6 — la mesure de §12.2 est confirmée au repos (le pass grille ne fait toujours aucun sous-solve moteur).
- **Après-solve légèrement plus haut sur P6** (70-82 ms) que sur P5 (60-68 ms), mais le **nombre** de longues tâches après solve ne suit pas (3/3/3 contre 2/4/4) et — point à ne pas escamoter — **les deux images ne diffèrent pas que de P6** : celle de `b0c36f3` est antérieure à U2-ter. Cet écart de ~12 ms n'est attribuable ni à P6 ni à U2-ter en l'état, et reste très en deçà du verrou.

Fichiers de mesure : `docs/qa/atelier-ui/p6-repos-{p6,p5}-{1,2,3}-cls.json` et les `*-longtasks.json` jumeaux.

**Conclusion : le socle P6 est déployable.** Il part avec le déploiement d'U3, comme prévu au §12.4.

### 12.6 Lecture finale (vérificateur, 09/09)

La mesure au repos de l'implémenteur (`fbc88cc`, six passages, `QA_OUT` hors OneDrive) est **validée** : après-solve 70-82 ms sur P6, aucun timeout, P6 pas plus lent que P5. Correction de ma propre lecture : les « 31-38 s » des tableaux sont `solveDoneAt` depuis le chargement de page ; la **durée de calcul** T-A au repos est **9 s** (« done (9s) » du harnais, runs du 08/09 matin), 21 s sous charge. Le socle P6 part avec U3 passe 2. Le lot 4 est **clos** sauf la phase C (import), non produite, reportée en tête de T3 — voir masterplan §4.

