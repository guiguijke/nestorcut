# Reprise du pilotage — point d'étape et plan pour le nouvel implémenteur (2026-09-13)

Document de passation écrit par le vérificateur à la demande du propriétaire
(changement d'agent implémenteur le 13/09). Il dit **où l'on en est, ce qui
tourne en production, ce qui reste, dans quel ordre, avec quelles règles** —
et renvoie aux documents qui font foi. À lire en entier avant le premier
commit.

## 0. Les rôles et la boucle de travail

- **Le propriétaire** (Guillaume) décide du produit, des priorités et de
  l'argent. Il relaie les rapports de l'implémenteur au vérificateur et
  inversement. Il ne lit pas le code : les messages sont courts, en français
  clair, avec un bloc « Pour l'agent » copiable.
- **L'implémenteur** livre **un lot = un commit (ou peu) + un rapport** dans
  le document du chantier (section « Rapports par lot »), avec : ce qui est
  livré, les verrous rejoués et leurs chiffres, les non-faits **dits
  franchement**, les points à arbitrer. Il **ne déploie qu'après le GO** du
  vérificateur. Forme des rapports : constat par constat, chiffres mesurés,
  aucune affirmation sans mesure (voir les rapports existants comme modèles).
- **Le vérificateur** (Claude, cette session) rejoue les verrous sur son
  poste, écrit une section « vérification » dans le même document, donne
  **GO / NO-GO**, tranche les arbitrages techniques (délégués par le
  propriétaire) et les consigne au registre du masterplan.
- La boussole : `docs/MASTERPLAN-2026-09-05.md` §0 (la référence du nesting
  dans le navigateur) et §8 (forme des instructions). La carte des documents :
  `docs/README.md`. Les pièges techniques : `AGENTS.md` §2 (à lire avant tout
  code moteur, worker, miroir JS ou visualizer). La page de suivi lisible du
  propriétaire : `docs/RESTE-A-LIVRER-2026-09-10.html` (priorités numérotées
  en clair).

## 1. Ce qui tourne en production le 13/09 au soir

| Composant | Commit / état | Source |
|---|---|---|
| app `app.nestorcut.com` + wasm géométrie + wasm moteur | **`baf39cb7`** (lot 2e), `:latest` promu sur ce SHA | `PLAN-IMPORT` §9.5 (note 2e) |
| worker fileprocessing | **`7622f46c`** (lot 2d) | `PLAN-IMPORT` §9.5 (note 2d) |
| worker nesting (Hetzner + 3 workers homelab) | lot **E2** (déployé le 13/09, note `53f93d2c`), homelab au même digest, `ASSERT OVERFLOW=HEAD: OK` | `PLAN-ECLATEMENT` §5 |
| page benchmarks publique | régénérée au `d890c92`, T-F 90/90 | `data/benchmarks.js` |
| services compose de prod | 6 services, **plus aucun « strip »** | `PLAN-RETRAIT-STRIP` §3.6 / §5 |
| tags d'images | `build-images.yml` publie `:<sha>` et `:main` ; **`:latest` ne bouge que par le workflow manuel `promote-latest`** sur un SHA qui a le GO | `PLAN-TAGS-IMAGES-2026-09-13.md` |

**Procédure de déploiement (D1, en vigueur)** : GO du vérificateur →
`promote-latest` (workflow_dispatch, SHA 40-hex) → sur la prod
`docker compose pull && docker compose up -d` (le `:latest` promu) → contrôles
de la note de déploiement (artefacts servis = dépôt au sha256, 0 ERROR,
`assert_overflow_head.py` si le worker nesting a changé, benchmarks
régénérés si le moteur a changé — `AGENTS.md` §6). Un déploiement
« worker nesting » se termine **toujours** sur le homelab.

## 2. Ce qui est clos (ne pas rouvrir sans mesure nouvelle)

| Priorité (page du propriétaire) | Résultat | Document |
|---|---|---|
| 1 — espacement tenu sur toutes les tôles | deux défauts (hole-fill navigateur ; embouchure du canal) corrigés, moteur qui mesure lui-même, démo 24/24 à 2,000 mm | `PLAN-DERNIERE-TOLE-2026-09-09.md` §17-§23 |
| 2 — import : garde, unités, messages, temps | 2a garde (10 000 entités, budget 20 s navigateur / 60 s serveur), 2b unités (table 0-20, flottants), 2c constats jusqu'à la fiche, 2d serveur 132 → 15 s, 2e navigateur 77 → 31 s ; corpus : 153 DXF réels + 85 versionnés, coureurs `scripts/qa-import-wasm.mjs` / `scripts/qa-import-ezdxf.py` | `PLAN-IMPORT-2026-09-09.md` §9 |
| 3 — pièces unitaires | E0 moteur robuste aux canaux fins + erreur d'item nommée ; E1 import avancé (éclatement, échelle, micro-vides) ; E1-bis aperçu sur une tôle ; E2 miroir serveur ; moteur × 238 fichiers : 0 refus. **ROUVERTE le 13/09 soir sur l'ergonomie : le propriétaire veut un interrupteur « Import avancé » à la création du projet et une fenêtre de choix à chaque dépôt (automatique par défaut / éclater + échelle), pas un panneau replié — lot E3, `PLAN-ECLATEMENT` §6, après J4-bis-2** | `PLAN-ECLATEMENT-2026-09-12.md` |
| retrait du pipeline « strip » | décision propriétaire 13/09, 23 fichiers supprimés, données conservées (il n'y en avait pas en prod) | `PLAN-RETRAIT-STRIP-2026-09-13.md` |
| D1 — tags d'images | `:latest` ≡ prod | `PLAN-TAGS-IMAGES-2026-09-13.md` |

## 3. Le chantier en cours : priorité 4, nester un `.job` SheetCam

Document unique : **`docs/ETUDE-JOB-SHEETCAM-2026-09-11.md`** — §1 format,
§2 neuf règles acquises par essais, §3 flux produit, §6 consigne de Phase 1,
§7 réponses du propriétaire, §8 prompt de mission, §9 rapports et
vérifications. Les fichiers d'essai sont **privés** (`.testparts/*.job`,
gitignoré) : ils ne sont jamais copiés ni nommés dans `docs/` (les fixtures
du dépôt sont des copies anonymisées, `app/tests/fixtures/sheetcam/`).

| Lot | État | Ce qu'il livre |
|---|---|---|
| J1 lecteur / écrivain | **GO** (13/09) | `shared/sheetcamJob.js` : lecture, écriture octet pour octet, copies `copyOf`, `[OpOrder]`, chemins masqués |
| J2 conversion de pose | **GO** (13/09) | `shared/sheetcamNest.js` : `XPos,YPos = t + R(θ)·c`, `Angle = −θ`, ordre de coupe (nichées d'abord), un `.job` par tôle |
| J3 réserve d'amorce et perçage | **GO sous conditions** (§9 de l'étude, vérification du 13/09) | `shared/sheetcamReserve.js` : espacement = kerf + 2 × 0,25, appendice local (enveloppe convexe sommet de départ + disque de perçage 32-gone), refus plutôt que géométrie fausse |
| **J4 branchement produit** | **livré le 13/09** (`a139e9f8`…`c3b13b33`), NO-GO du 13/09 levé par **J4-bis `ddaa7b0d`, vérifié le 13/09 soir : GO déploiement (app + worker nesting) dès le verdict machine du propriétaire** — étude §9.36 | dépôt d'un `.job` + ses DXF, réglages pré-remplis, réserve d'amorce des TROUS (le verdict de la recette est traité), un `.job` par tôle téléchargeable, angle normalisé, libellés FR/EN, harnais `scripts/qa-e2e-job.mjs`. Rapport : étude §9.15 à §9.23. **Un défaut OUVERT au §9.22** (une pièce sur deux posée avec la réserve) et la recette machine non rejouée : les deux bloquent le déploiement |
| recette SheetCam | **verdict rendu le 13/09** : pièces et ordre de coupe **OK** ; **NOT OK : l'amorce du contour du trou de l'hôte part vers l'intérieur du trou et coupe les éventails nichés** (voir §9 de l'étude, « Recette SheetCam — verdict ») — à résoudre au lot J4 avant tout déploiement ; fichier (`.testparts/RECETTE_nestorcut_x4.job`, produit depuis un vrai nesting serveur : 5 pièces, 4 éventails dans le trou, écart 2,000 mm) ; verdict consigné en §9 de l'étude | — |

Rien de J1-J3 n'a d'appelant produit : **la production ne change pas** tant
que J4 n'est pas livré.

## 3-bis. Où en est le lot J4 (mise à jour du 13/09, après le J4-bis)

| Étape | État |
|---|---|
| J4 branchement produit | livré (`a139e9f8`, `165251a8`, `c3b13b33`) |
| vérification | **NO-GO** du vérificateur — étude §9.24 à §9.28 |
| **J4-bis correctif** | **livré `ddaa7b0d`** — étude §9.29 à §9.34 |
| **vérification J4-bis** | **GO** (étude, section « Lot J4-bis — vérification ») : 5 sur 5 et 2 sur 2 rejoués sur image reconstruite, vitest 715, pytest 241 en conteneur ; le miroir Python part AVEC ce lot (défaut latent en prod), homelab compris |

Le défaut du NO-GO est corrigé et **mesuré dans le navigateur, sur image
reconstruite et sans double dépose** : le `.job` à 2 pièces pose 2 sur 2 (il
posait 1 sur 2), la recette 1 hôte + 4 éventails pose 5 sur 5 (elle posait
4 sur 5), tous les verrous verts dans les deux cas. vitest 715, pytest
worker nesting 241 passés / 1 ignoré, `nuxt build` et
`docker compose build app` verts.

Il ne s'agissait pas d'un défaut du `.job` : `reduceForSolve` retirait des
pièces de l'instance moteur en pariant que l'expansion les rattacherait au
CENTRE des trous, et le pari est faux sur tout trou non symétrique — donc
**latent en production** pour un trou en L ou en C avec un seul fichier hôte
et un seul fichier de remplissage. La forme compressée n'est désormais prise
que si elle porte le plan, une garde anti-perte écarte l'alternative si le
compte ne tombe pas juste, et le miroir Python suit.

**Ce qui reste avant de déployer**, dans cet ordre :

1. **recette machine du propriétaire** : ouvrir dans SheetCam
   `.testparts/RECETTE-J4bis_x4.job` (ou `VERIF-J4bis_x4_5sur5_tole1.job`,
   même version) et dire si l'amorce du trou touche encore l'éventail
   niché. Attendu : 5 pièces, UN éventail dans le trou (les quatre morsures
   ne laissent la place qu'à un), l'éventail coupé avant l'hôte ;
2. **J4-bis-2 livré `49beecfa`/`eabaee9d`, vérifié le 13/09 nuit : GO sous deux conditions (étude §9.44 à §9.46)** — J4-bis-3 (appariement bloc ↔ dessin par la géométrie, le rang casse sur un `.job` réordonné) et la réponse du propriétaire « SheetCam perce-t-il sur un POINT ? » ; puis recette, puis déploiement **app + worker nesting** par SHA approuvé et `promote-latest`
   (AGENTS §6), homelab compris (`assert_overflow_head.py`),
   `densities_corpus.py` rejoué. Si le verdict est NOT OK : le worker part
   seul, l'app attend le correctif ;
3. puis : **lot E3** (interrupteur « Import avancé » + fenêtre de choix au dépôt, `PLAN-ECLATEMENT` §6, app seule), remettre le cas A4 du harnais APRÈS le nesting, identifier les deux
   « 400 Bad request », et le lot J4-ter à réception de la série de G-codes
   (§5-bis).

**À savoir avant de rejouer le harnais** : il crée un projet par exécution, et
enchaîner les lancements déclenche la limite anti-force brute (429,
piège #43) ; le refus qu'on lit alors n'a rien de géométrique.

## 4. Première action du nouvel implémenteur : réparer le dépôt

> **FAIT le 13/09** (`b96813fb`) : les 86 fichiers sont restaures, et
> `git diff --stat baf39cb7 HEAD -- docs/qa` ne montre plus aucune
> suppression. La section reste ici pour memoire de la regle.

Le commit de correction `f95582d7` (« retirer du suivi 360 artefacts QA ») a
aussi **retiré du suivi 86 fichiers qui étaient légitimement versionnés
avant le lot J3** (69 dans `docs/qa/atelier-ui/`, 12 dans
`docs/qa/pr5-acceptation/`, 5 dans `docs/qa/audit-multitoles-2026-09-03/`),
dont les rapports `RAPPORT-P4/P5/U2.md` et les mesures référencées par
`docs/FICHE-LOT4-T2-2026-09-06.md`. Les fichiers sont encore sur le disque
du poste précédent, et dans l'historique. À faire, en un commit, **sans
réécrire l'historique** :

```bash
git diff --name-only --diff-filter=D baf39cb7 f95582d7 | xargs git checkout baf39cb7 --
git status --short            # exactement 86 fichiers ajoutés, rien d'autre
git commit -m "fix(depot): restaurer 86 artefacts QA versionnés retirés par erreur (f95582d7)"
```

Puis vérifier que `git diff --stat baf39cb7 HEAD -- docs/qa` ne montre plus
aucune suppression. Règle (AGENTS §7) : **jamais `git add -A`**, des chemins
explicites, `git status` avant chaque commit.

## 5. Lot J4 — brancher le `.job` au produit (consigne fermée)

Objectif : un utilisateur dépose un `.job` SheetCam, NestorCut neste ses
pièces avec l'espacement et les réserves qui en découlent, et il télécharge
un `.job` par tôle que SheetCam ouvre avec le bon ordre de coupe. Chemin
navigateur d'abord (masterplan §0), un seul code (`shared/`).

1. **Dépôt** : `.job` accepté dans `DxfUpload.vue` (signature, pas
   l'extension — piège #31 ; un `.job` = texte INI + `[BinaryDataStart]`).
   Les dessins référencés (`DrawingFile`) ne sont pas dans le `.job` : la
   fiche demande les DXF correspondants s'ils manquent (par nom de fichier,
   règle 9), ou les prend s'ils sont déposés ensemble. Une fiche par dessin,
   quantité = nombre d'exemplaires dans le `.job`.
2. **Réglages pré-remplis** (modifiables, jamais imposés) : espacement =
   `spacingFromKerf(kerf)` ; tôle = `[Work]` ; `pierceMarginMm` réglage
   utilisateur, défaut `DEFAULT_PIERCE_MARGIN_MM` (3 mm, **valeur à confirmer
   par le propriétaire**) ; table `Start position → coin` (`START_CORNERS`)
   **à confirmer par un essai SheetCam**, sinon repli sur la plus longue
   arête droite (déjà codé).
3. **Nesting** : les polygones nestés sont `partWithReserve(part)` (réserve
   locale), le `.job` rendu garde les contours réels ; `nestedIn` vient du
   post-pass (`holesFilled`, membres par trou), pas d'une déduction
   géométrique.
   **Réserve d'amorce des TROUS (défaut constaté à la recette du 13/09)** :
   l'amorce et le perçage d'un contour intérieur tombent DANS le trou, là
   où le remplissage pose des pièces ; il faut soit réserver au point de
   départ prédit du trou une zone d'exclusion vers l'intérieur (amorce +
   disque) soustraite de la zone libre du remplissage, soit imposer le point
   de départ du trou après nesting là où il reste de la place, si le format
   le permet. Détail et verrous : étude §9, « Recette SheetCam — verdict ».
   Rien n'est déployé tant que la recette machine rejouée n'est pas OK.
4. **Sortie** : un bouton « Télécharger le .job » par tôle
   (`nestedJobsPerSheet`), nom `<nom>_tole<k>.job` ; **normaliser l'angle
   écrit dans (−π, π]** avant l'écriture (le fichier de recette porte
   `Angle=-6.283` pour un θ = 2π émis par le moteur — à régler côté J2/J4,
   une ligne) ; constats des réserves refusées (`part.reserve.reason`) dans
   la fiche, FR et EN.
5. **Langues** : tout libellé dans `app/utils/i18n.js`, anglais ET français
   (`i18nDict.test.js` attrape une clé manquante).
6. **Verrous** : vitest ; harnais navigateur dédié (`scripts/qa-e2e-job.mjs`,
   fichiers privés passés par variable d'environnement, copies anonymes en
   sortie) : dépôt d'un `.job` + ses DXF → nesting → `.job` téléchargé
   **identique à la structure de la recette** (Count, `copyOf`,
   `Optimisation=3`, `[OpOrder]` nichées puis hôte, chemins masqués, bloc
   binaire intact) ; harnais 900 pièces deux configurations inchangé pour
   les jobs sans `.job` ; déterminisme natif ≡ wasm intact (aucun code
   moteur). **Recette machine** : le propriétaire ouvre le fichier produit
   dans SheetCam ; son verdict va en §9 avant tout déploiement.
7. Miroir serveur (« Nos serveurs ») : lot J5, après J4, même plomberie
   qu'au lot E2 (champs additifs sur le document fichier, worker
   fileprocessing).

## 5-bis. Lot J4-ter — les amorces reconstruites, calées sur le G-code (décision du 13/09 soir)

Après J4-bis. Le propriétaire fournit une série de `.job` du même DXF avec
tous les types d'amorce et leur `.tap` (protocole : étude §9.35). Livrable :
lecteur `.tap` minimal, module `shared/sheetcamLeads.js` (enveloppe prédite
depuis les clés du `.job` : kerf/2, amorce d'entrée et de sortie, coin de
départ mesuré), verrou Hausdorff ≤ 0,1 mm par fichier de la série, réserve de
nesting = cette enveloppe. Tant que la série n'est pas là, la réserve
prédictive de J4 reste en place.

**Mise à jour du 13/09 soir (étude §9.37 à §9.39)** : la recette J4-bis
est OK sur l'amorce du trou ; le propriétaire ajoute l'option
`allowOverlappingLeads` (défaut non) ; les trois premiers fichiers de la
série ont livré les règles d'arc (quart de cercle, rayon 0,64 × longueur,
entrée ET sortie, perçage en P − r·t + r·n) et **le codage des points de
départ dans le bloc binaire** (couples de doubles relatifs au centre de la
pièce, pas de 179 octets) — la voie « imposer le point de départ » rouvre.
Périmètre J4-ter mis à jour au §9.39 ; le reste de la série est listé là.

**Fin de soirée du 13/09 (étude §9.40)** : espacement `.job` =
**2 × kerf + sécurité (1 mm par défaut)**, à livrer en **J4-bis-2 AVANT le
déploiement** ; `Pièce L-1.job` porte les points de départ par défaut
(extérieur : coin bas gauche ; trous : ni coin ni milieu, règle non
élucidée) ; J4-ter commence par la lecture/écriture des points de départ
dans le binaire, l'enveloppe des amorces ensuite.

**19 h** : série presque complète (étude §9.41) — douze `.job`, dix `.nc` ;
règles d'amorce lues pour les quatre types, kerf/2 confirmé à trois kerfs,
ordre des enregistrements du binaire établi (rectangle, extérieur, fuseau)
avec un octet de drapeau à +87 par enregistrement quand le point est déplacé
à la main ; la référence « défaut » est « none default », pas L-1. Manquent :
deux `.nc` et les quatre fichiers de coin.

**19 h 30, série complète (étude §9.42)** : trois faits. La formule de pose
du lot J4 est fausse (45 mm à 45°), celle de J2 était juste — **NO-GO
jusqu'au retour à J2** ; `Start position` (0 centre, 1 haut gauche, 2 haut
droit, 3 bas droit, 4 bas gauche) est l'origine de l'ORDRE de coupe, pas le
départ des contours — le vrai départ est dans le binaire, à jour ; la
réserve aux quatre coins ne couvre pas un trou rectangulaire. J4-bis-2
révisé en cinq points au §9.42 ; J4-ter réduit à l'écriture du point.

**Nuit du 13/09 (étude §9.43 à §9.46)** : J4-bis-2 livré et vérifié —
pose J2 rétablie (0,750 mm sur le 45°), espacement 2 × kerf + sécurité
étendu à tout le formulaire (arbitrage owner attendu, avis favorable),
lecteur du binaire en flux tag/longueur (39/39), réserve au point lu.
Découvert : une entité POINT au centre du trou de la recette ⇒ trou retiré
tant que le propriétaire n'a pas dit si SheetCam y perce. À corriger avant
déploiement (J4-bis-3) : l'appariement bloc ↔ dessin par rang casse sur un
`.job` dont les sections ne suivent pas l'ordre de chargement (« ordre_TEST »,
`F1` en échec) — apparier par la géométrie.

**J4-bis-3 `ea6bc5d5` vérifié (étude §9.48-§9.49) : GO** — appariement par
la géométrie juste sur le fichier « ordre » ; reste avant déploiement :
J4-bis-4 (lever le plafond de 7 dessins par une affectation gloutonne) et la
réponse du propriétaire sur le POINT. **Réponse reçue : voir ci-dessous.**

**G-code du propriétaire lu (étude §9.50)** : SheetCam ne perce pas sur un
POINT (six perçages pour six contours) ⇒ la règle « perçage errant » tombe ;
et le point de départ AUTOMATIQUE du binaire est un cache recalculé par
SheetCam (deux fichiers à 90° du réel) ⇒ **J4-ter (écrire le point, drapeau
« déplacé ») conditionne le déploiement**. Ordre : J4-bis-4 → J4-ter →
vérification → recette → déploiement ; E3 en parallèle.

**Marges (owner, 13/09 nuit, étude §9.51)** : disque de perçage = 2 × kerf
(plus de constante 3 mm), bande ± kerf autour des trajets d'entrée et de
sortie ; à livrer avec J4-ter.

**J4-ter `2e4109a5` vérifié (étude §9.53) : GO** — points écrits avec le
drapeau, 3 octets sur le cache de la recette, harnais verts ; déploiement
après la recette machine (G-code amorçant là où NestorCut a écrit, et sur le
fichier à trois points choisis du vérificateur). **E3 `35b27b0a` vérifié
(`PLAN-ECLATEMENT`, section « Lot E3 — vérification ») : GO** après un cas
serveur G au harnais. Hygiène : l'étude est repassée en CRLF au commit
`2e4109a5`, à remettre en LF.

**Recette machine J4-ter OK (13/09, 23 h, étude §9.54)** : le G-code amorce
aux points écrits à kerf/2 près, rotation comprise. **Déploiement `.job`
autorisé** (app + worker nesting), E3 avec son cas G. Question ouverte au
propriétaire : 1 mm entre les bandes de kerf (règle actuelle) ou 2 × kerf
entre les bandes (`4 × kerf + sécurité`).

**14/09 — J4-ter et E3 EN PRODUCTION (`9b227ec1`), contrôlé par le
vérificateur (étude §9.57)** : `gitCommitSha` exposé par la page, route E3
en place, homelab à HEAD, benchmarks non réécrits (T-F : fourchette 88-90,
décision de page pour le propriétaire). Suite : **J4-quater « meilleur point
de départ »** (conception au §9.58, passe après solve, écriture avec
drapeau), puis J5, puis la couture.

**14/09 — PRIORITÉ 1 : correctif H1, la vue DXF d'un résultat local fige la
page** (`docs/PLAN-CORRECTIF-VUE-DXF-2026-09-14.md`) : `uniquifyDxfHandles`
prend une VALEUR `5` (couleur du calque `BIN_BOUNDARY`) pour un code de
handle et casse le `ENDTAB` ; dxf-viewer boucle. Régression depuis le lot 2b
(12/09, entiers écrits en entiers). Correctif par paires (code, valeur),
verrous unitaires + vue DXF au harnais, app seule. Puis lot E4 (§7 du
`PLAN-ECLATEMENT`), après accord du propriétaire.

**H1 déployé `d8f7fc69` (14/09), vérifié a posteriori : GO** — reproduction
rejouée, page réactive à +60 s ; déploiement fait avant le GO, remarque
consignée au §5 du plan correctif. **Suite : lot E4 dès l'accord du
propriétaire sur `PLAN-ECLATEMENT` §7.**

**14/09 — lot E4 ouvert (accord du propriétaire : « un dessin non éclaté
nesté comme un bloc rigide, oui tout à fait »)** : consigne fermée
`PLAN-ECLATEMENT` §8 — E4-a bloc rigide (enveloppe convexe, un item, aire
vraie au rapport, miroir Python) d'abord ; puis E4-b « Échelle » sur la fiche
(saisie largeur/hauteur, poignée), E4-c « Éclater » sur la fiche, E4-d retrait
de la fenêtre et de l'interrupteur. Après E4 : J4-quater, J5, couture.

**Règle du propriétaire (14/09, étude §9.59)** : un point de départ marqué
« déplacé à la main » dans le `.job` est INTOUCHABLE — réserve posée dessus,
jamais réécrit ; seuls les points automatiques peuvent être choisis par
NestorCut. À rendre explicite dans le code avec son verrou (fichier « start
rectangle moved » octet-identique) — petit lot à glisser avant J4-quater.

**E4-a `c8fe187d` vérifié (14/09) : GO** — 1 item convexe, handles union,
aire vraie au rapport, contrôle négatif mono-pièce, harnais bloc vert ;
déploiement app + worker nesting (homelab) avec E4-b/c/d ou seul.

**E4-b/c/d `8b87e678` vérifié (14/09) : GO déploiement** — échelle et éclatement
sur la fiche, fenêtre et interrupteur retirés ; déploiement E4 complet
(app + worker fichiers + worker nesting pour E4-a, homelab) après ce GO.

**E4-e (owner 14/09)** : faisabilité d'une enveloppe mixte convexe/concave
du bloc — mesure vérificateur : fermeture au r minimal connexe = 28 % du
convexe sur le logo (2,5 × l'aire vraie au lieu de 8,9) ; étude d'un jour,
sans changement de production, `PLAN-ECLATEMENT` §9 ; après le déploiement
d'E4, avant ou en parallèle du lot « point déplacé intouchable ».

**14/09 — GEL DES NOUVELLES FONCTIONNALITÉS (décision du propriétaire)** :
avant E4-e, J4-quater, J5 et la couture, le propriétaire fait lui-même la
recette des priorités 3 et 4 en production :
`docs/RECETTE-PROPRIETAIRE-2026-09-14.md` (tableaux A à C). Jusqu'au
verdict, l'agent ne livre que des correctifs issus de cette recette.
E4 déployé `8b87e678` (contrôle vérificateur : page à ce SHA, homelab HEAD).

**Lot « point déplacé intouchable » `117bb4ce` vérifié (14/09) : GO** (app
seule), étude §9.60 ; puis recette du propriétaire, gel des features.

**14/09 — audit de bout en bout des priorités 3 et 4, exécuté par
l'implémenteur** (`docs/AUDIT-E2E-PRIO3-4-2026-09-14.md`, 14 + 14 + 4
scénarios, deux jours, aucun correctif mêlé) ; le vérificateur rejoue un
échantillon ; les rouges deviennent des lots courts. En parallèle : recette
du propriétaire en production. Gel des nouveautés maintenu.

**Audit E2E exécuté (14/09, `c78e7ed5`)** : 26 verts, 2 rouges ⇒ lots A1
(refus actionnables, `643fb2da`, **vérifié GO, app seule**) et A2 (faux
positif du harnais, accepté) ; P4-10 attend le `.job` réel d'atelier.
Hygiène à corriger : noms de fichiers du corpus dans deux harnais committés
et dans le §5 de l'audit. Décision owner ouverte : suppression de fiche
(P4-12).

**14/09 — le `.job` porte la géométrie** (étude §9.61 : segments lignes/arcs
décodés du binaire, vérifiés sur le carré à trou) ⇒ lot J6 « `.job` seul »
possible (2 j), décision owner sur sa place par rapport au gel.

**Décision owner 14/09 : le `.job` se dépose SANS son DXF** ⇒ **lot J6
« `.job` seul » (étude §9.62) passe avant la levée du gel**, juste après le
déploiement d'A1 : décodage des segments du binaire, DXF canonique par
dessin, import ordinaire, DXF déposé prioritaire, refus nommés, verrous de
coïncidence sur 18 fichiers, cas « `.job` seul » au harnais. Recette C1 mise à
jour.

**J6 `dad20367` vérifié (14/09 soir) : GO déploiement app seule** — décodage indépendant
70 couples (66 exacts, 4 croisés = fichiers « ordre »), ellipse 0,0996 mm,
harnais quatre passages verts ; arbitrages : seuil 0,12, liaison par rang,
message serveur ; réserve J6-bis : le DXF déposé APRÈS coup ne remplace pas
la fiche issue du binaire (doublon).

**J6-bis `777b48a8` vérifié (15/09) : GO a posteriori, réserve levée** —
rejeu navigateur indépendant : `.job` seul puis `.job` + DXF ⇒ 2 fiches,
mêmes slugs, mêmes rangs, quantités 1 et 4 intactes ; inter-projets sûr ;
hygiène faite ; vitest 768. **En production** (page à `777b48a8`), la recette
C1 se joue avec le `.job` seul. Constat neuf non bloquant : le chemin de
remplacement court-circuite les refus d'entrée (un `.dwg` déposé sous le nom
d'un dessin rend un message générique au lieu du message actionnable) ⇒ lot
J6-ter, étude §9.66.

**J6-ter `3256f706` vérifié (15/09) : GO app seule** — le plafond de taille
s'exécute maintenant sur le chemin de remplacement (DXF de 6,3 Mio sous le
nom d'une fiche du binaire ⇒ refus, fiche intacte) ; non-régression J6-bis
verte (2 fiches, mêmes slugs, quantités 1 et 4) ; vitest 769. **Rectificatif :
ma mesure du §9.66 comparait deux extensions et non deux chemins** — l'écart
de message publié venait du nom du fichier ; le défaut de fond et le
correctif restent valides (étude §9.68).

**15/09 — six `.job` « points de départ » du propriétaire, mesurés (étude
§9.69) et lot J7 ouvert.** Décodage 6/6, coïncidence aux DXF à 0,00000 mm de
étendue, points manuels respectés à l'octet (0 changé) contre 3 drapeaux pour
les automatiques. Fait nouveau : **9 contours sur 11 ont leur départ à
l'intérieur d'une arête**, automatiques compris. **Défaut trouvé, antérieur à
J6 : une pièce seule de moins de 5 000 mm² sur la tôle du propriétaire est
REFUSÉE** (garde #2b, bande initiale dégénérée) et le conseil affiché est
inapplicable — ajouter du stock ne change rien tant que la direction est
« gauche seule ». J7-a = correctif (bascule multi-tôles côté payload, sans
toucher au moteur), J7-b = verrous de la série. P4-10 toujours ouvert.

**J7 `efa0e37e` vérifié (15/09) : GO app + worker nesting + homelab**, avec
J6-ter dans la même promotion. Les deux petites pièces qui refusaient se
nestent (1/1, résultat utilisable : pièce en coin, badges verts, chute
réutilisable), le point manuel sur l'hypoténuse survit à l'octet, le chemin
serveur bascule aussi, non-régression et banc de référence inchangés ; vitest
769, pytest 251. Aucun diff moteur/wasm ⇒ benchmarks sans objet. Arbitrage :
le marqueur `__spacingTooLarge`, devenu inatteignable, part avec le prochain
lot qui touche ces fichiers (étude §9.71).

**Déploiement J7 + J6-ter contrôlé (15/09) : conforme** — prod à `efa0e37e`,
`:latest` app et worker au MÊME digest que ce SHA, homelab mesuré par le
vérificateur (digest identique, `md5(main.py)` = HEAD). Le `headSha` du
journal `promote-latest` (`0d4ad725`) est la tête de branche au lancement, pas
le SHA promu — seul le digest fait foi. **Point d'infra à trancher** :
`mongo:7` est une étiquette flottante, ce déploiement a recréé la base de
production sans que ce soit voulu — épingler la version et ne tirer que les
services déployés (étude §9.72).

**15/09 — demande du propriétaire, lot J8 ouvert (étude §9.73)** : « si je
mets des `.job` dans l'outil je m'attends à télécharger un `.job` en sortie »
et « prévisualiser les lead in / lead out dans la liste des parts et pendant
le nesting ». Constaté : le DXF est le bouton PRIMAIRE même sur un projet
issu d'un `.job`, et le `.job` multi-tôles ne rend que la tôle affichée
(asymétrie relevée par le vérificateur). La géométrie d'amorce existe déjà
(`leadPathLocal` + repères de `biteAtStart`), il manque la sortie monde et le
rendu. J8-a = boutons, J8-c = provenance `.job` sur les cartes,
**J8-d = l'écran de création dit que « Cet appareil » neste les `.job` ET Y CONDUIT (un `.job` déposé en mode serveur bascule le projet en mode appareil et le dit ; `.job` + `.dwg` = refus nommant les deux). Textes figés par le vérificateur sur délégation du propriétaire (15/09), plus une ligne `[prod]` dans STRATEGY ; le site de présentation est un lot séparé APRÈS la mise en production. (Il
dit aujourd'hui le contraire : la carte appareil ne parle que de ses limites
et la carte serveur se vend comme la plus capable, alors qu'elle REFUSE le
`.job`)**, J8-b = amorces dans la liste des fiches et la vue live. **J8-e ajouté après une question du propriétaire (« et si mon client a plus de 20 fichiers ? ») : la limite de 20 N'EXISTE PAS en mode appareil (jamais filtrée côté client) et, en mode serveur, un dépôt qui la dépasse échoue EN SILENCE — `uploadToServer` avale toutes ses erreurs dans un `console.error`, coffre verrouillé et quota compris. Correctif : envoi par lots automatique, erreurs affichées, légendes sans compte. J8-e passe EN PREMIER (défaut muet en production).** ; l'aperçu couleur du résultat est un lot à part (fichier SVG produit,
parité Python). **Aucun impact sur le calcul** ⇒ avance pendant la recette ;
le gel tient pour tout ce qui change un résultat.

**J8 `c2b0fc23` vérifié (15/09) : NO-GO, lot J8-bis court (étude §9.75).**
Beaucoup de bon et mesuré (envoi par lots + erreurs nommées, provenance `.job`
sur les cartes, amorces dans l'aperçu, multi-`.job` réparé). Mais : les points
22-23 de la consigne ne sont PAS faits et le rapport ne le dit pas, si bien
que l'écran se CONTREDIT — la carte annonce « .job acceptés » et le dépôt
répond « type de fichier non supporté » ; un `.dwg` mêlé à un `.job`
disparaît sans un mot. Et deux verts faux : **vitest sort en code 1**
(3 exceptions non rattrapées, `a.remove` absent du faux élément) rapporté
« 783/783 », et le harnais de l'implémenteur a lui-même **un verrou rouge**
rapporté vert. Règle posée : le verdict d'une suite est son CODE DE SORTIE ;
un point de consigne non fait se DIT.

**15/09 — lot J9 ouvert (étude §9.76), défaut de MODÈLE trouvé sur un
fichier du propriétaire.** Un `.job` où le même dessin a QUATRE sections
originales, chacune avec son point de départ déplacé à la main. Mesuré : le
binaire porte **un bloc par SECTION ORIGINALE**, pas par nom de dessin (la
règle 6 dit bien « par rang », c'est mon implémentation J6 qui a lu « rang
des noms ») ; et les blocs 2 à 4 portent une **sentinelle d'origine** (3e38)
signifiant « même origine que le précédent », que notre décodeur additionne.
Conséquence mesurée : déposé seul le fichier est REFUSÉ en entier ; déposé
avec son DXF, les quatre points de départ sont PERDUS (`startNotRead`) et le
`.job` rendu porte 7 sections pour 4 pièces. Cause profonde : notre modèle
porte un jeu de points PAR DESSIN, SheetCam en porte un PAR PIÈCE. J9 : une
fiche par section originale. Après J8-bis, avant toute nouveauté.

**15/09 — exigence du propriétaire : « l'import et l'export de `.job` doivent
être parfaits, il faut parfaitement comprendre le fichier ».** Mesuré avant de
planifier (étude §9.77) : l'aller-retour sans modification rend **54 fichiers
sur 54 IDENTIQUES À L'OCTET** — tout ce que nous ne comprenons pas est déjà
préservé. Inventaire : **38 champs binaires, 19 décodés, 19 inconnus**, mais
**2 seulement VARIENT** (`0x14`/`0x15`, hypothèse « types d'amorce » testée et
FAUSSE), 9 sont constants sur les 54 fichiers et 8 n'existent que dans j9-1.
Côté texte, `[Work/keepout]`, `[Table]`, `[Cutoff]`, `[pathRules]`,
`[ToolChange]`, `[Variables]` ne sont pas lus. Lot J10 en trois marches :
verrouiller l'acquis, épuiser les paires de la série existante, puis une
liste courte d'expériences pour le propriétaire (keepout d'abord).

**J8-bis + J9 + J10 `5cffa2f6` vérifiés (15/09) : GO app seule, avec un
J9-bis obligatoire** (étude §9.79). Acquis mesurés : vitest **code de sortie
0** (le faux vert est mort), la sentinelle d'origine résolue (4 blocs à
(60 ; 60), 4 points réels), **4 fiches « (k/4) »** pour le fichier à quatre
originales, la bascule vers « Cet appareil » qui lève la contradiction
d'écran. Deux défauts à corriger : le refus `.job` + `.dwg` ne tient QU'EN
mode serveur — en mode appareil le `.dwg` disparaît sans un mot et le projet
se crée (cause : `uploadExtensions` l'écarte avant `handleSubmit`) ; et le
verrou d'aller-retour ignore les 4 `.job` du DÉPÔT, les seuls présents en CI,
où il ne mesure donc rien.

**Déploiement `5cffa2f6` contrôlé et J9-bis `711f4b44` vérifié (15/09) : GO**
(étude §9.81). Prod au bon SHA, `:latest` au MÊME digest, app seule. J9-bis :
le mélange `.job` + `.dwg` est refusé dans les DEUX modes sans projet créé, un
DWG seul en mode appareil est nommé sans projet muet, les légendes restent
propres à leur mode malgré l'union au filtre, suite à **789 / exit 0**, et le
verrou d'aller-retour tourne enfin sur les fixtures du dépôt (donc en CI).
Zéro verrou rouge à ma mesure.

**J9-bis `711f4b44` déployé et contrôlé (15/09) : conforme** (étude §9.82) —
prod au bon SHA, `:latest` au MÊME digest, app seule. **La chaîne `.job` est
complète en production.** Plus rien en attente côté implémenteur ni
vérificateur : il reste la recette du propriétaire (C et C-bis, fichier à
quatre pièces compris), la liste d'essais de la carte du format (zone
d'exclusion en premier) et P4-10 à réception du `.job` d'atelier.

**15/09 — audit UI/UX du flux `.job` et lot J11 (`docs/PLAN-UX-JOB-VERSION-2026-09-15.md`).**
Parcours complet à HEAD. Défaut haut : **le projet créé depuis le fichier à
quatre pièces s'appelle « 4) »** — le suffixe « (1/4) » de J9 contient une
barre oblique que `titleFromFileName` prend pour un chemin. Remarque du
propriétaire (quatre cartes au lieu d'une) tranchée : présentation GROUPÉE
(une carte « ×4 » dépliable), modèle J9 conservé, PAS de bloc rigide — dans un
`.job` les sections sont déjà des pièces libres pour SheetCam, il n'y a rien à
éclater. Aussi : « 4 fichiers » pour un fichier, deux « densité » de sens
différents côte à côte, la vue agrandie d'une fiche sans amorces. Plus deux
souhaits du propriétaire planifiés : version produit affichée (V2.0 proposé
pour la prod actuelle, MINEUR à chaque promotion, `CHANGELOG.md`) et badges
« Nouveau » à expiration par date, sans suivi utilisateur.

**15/09 — décisions du propriétaire : la production est V0.9** (V1 est son
jalon), journal des nouveautés dédié à l'app (`CHANGELOG.md`, une source),
carte groupée SANS bloc rigide confirmée (« on éclate par défaut »), et
**ouverture d'un chantier documentation** complet (menu, recherche,
multilingue, interface et nesting expliqués) — plan
`docs/PLAN-DOCUMENTATION-2026-09-15.md` : Starlight dans le site vitrine Astro
existant, FR d'abord + EN dans le même lot, chaque phrase vraie en prod,
captures par harnais sur fichiers neutres, relecture du propriétaire section
par section, lots D1 à D5. J11 mis à jour (V0.9, `0.9.x` par promotion,
CHANGELOG). La consigne J11 précédente n'avait pas été transmise : celle-ci en
tient lieu.

**J11 `ba4dbad9` vérifié (15/09) : NO-GO, lot J11-bis**
(`docs/PLAN-UX-JOB-VERSION-2026-09-15.md` §4). Juste et mesuré : nom du projet
(plus de « 4) »), carte groupée « ×4 » avec « votre point », en-tête « 1
fichier déposé », les deux densités nommées, « V0.9 » dans l'en-tête, journal
créé. Ne tient pas à l'écran : **la carte groupée est cassée** (largeur d'une
carte simple, quatrième vignette coupée), **la page Nouveautés tronque chaque
puce à sa première ligne et affiche le Markdown brut** (titre en anglais),
**le badge « Nouveau » n'est posé nulle part**, le slug technique est toujours
dans le modal, la version manque au pied de page de l'app, la barre de résumé
dit encore « 4 fichiers », le bouton « Support » est bien dans `auth.vue:33`.
Découverte annexe : **le serveur rend l'anglais avec un cookie français, en
production aussi** (éclair d'anglais ancien) — J11 a rendu le mismatch
visible, le correctif est côté serveur. Règle posée : un lot d'interface se
rapporte avec ses captures, regardées.

**Chantier langues ouvert le 16/09 (décision owner : pt-BR, it, de, es) — `docs/PLAN-LANGUES-2026-09-16.md` ; L0 (socle : un fichier par langue, verrou de parité, Intl, Accept-Language, menu) peut démarrer en parallèle de D4 ; L1 portugais après D4 publié.** **GO DE FUSION donné le 16/09 (relecture owner : « la doc me paraît OK ») : PR #14 `77fcbce` → publication nestorcut.com/docs + /fr/docs, contrôle en ligne à suivre, puis D4 sur branche neuve.** **Retouche `77fcbce` contrôlée (16/09) : légende FR du dessin entière, viewBox 660 servi — seule reste la relecture du propriétaire avant le GO de fusion.** **D3-bis `df1beb7` vérifié le 16/09 : GO technique pour publier les sections
1 à 4, sous deux conditions — relecture du propriétaire (d'abord la page de
l'espacement, sa règle, puis la page `.job`) et un attribut du dessin FR
(légende coupée au bord du `viewBox`).** Build 53 pages et liens en code 0,
cinq corrections vraies, ligne d'état = libellé réel de l'app, dessin rendu et
regardé dans les deux langues. Après fusion : contrôle en ligne, puis D4 sur
une branche neuve.

**D3 (documentation, même PR, `444e64f`) vérifié le 16/09 : sections 1 à 4
complètes, cinq corrections courtes (D3-bis) puis relecture owner et
publication** (plan documentation, section « Vérification du lot D3 »).
Build 53 pages et liens en code 0 ; menu prouvé dans le HTML bâti (lien simple,
chaque page une fois, ordre de lecture) ; 16 images par langue, EN en anglais,
FR en français, regardées ; neuf corrections de D2 faites ; les onze pages
nouvelles contrôlées contre l'app, vraies. À corriger : le dessin et
l'explication de l'espacement (un seul kerf dessiné pour une règle à deux —
explication à valider par le propriétaire), les exports CSV/copie verrouillés
en gratuit non dits, « Arrêter » qui n'existe pas (« Annuler »), la condition
de la bande incomplète (une tôle ou bord gauche), « du trade ». Après GO :
fusion de la PR, publication sur nestorcut.com/docs ; D4 sur une nouvelle
branche.

**D2 (documentation, même PR, `3b6948e`) vérifié le 16/09 : contenu solide,
neuf corrections absorbées dans D3** (plan documentation, section
« Vérification du lot D2 »). La neuvième, relevée par le propriétaire : les
pages anglaises montrent des captures en français — règle corrigée, une
capture par langue (`docs-img/fr/`, `docs-img/en/`). Build 39 pages et liens rejoués en code 0,
douze routes, huit images toutes référencées, captures propres et regardées
(compte neuf, cadrées), page `.job` exacte, corrections D1 faites. À
corriger dans D3 : menu doublé (sous-groupe brut « files » sous
« Démarrer », `autogenerate` récursif — le rapport disait le contraire),
« une proposition par sens » faux en gratuit (1 sens), « Écart ≥ 4 mm »,
contour ouvert (les tracés ouverts ne sont PAS découpés), échelle/éclatement
sur la carte et non dans la vue agrandie, épaisseur `.job` non utilisée,
DWG (fiche en erreur « Échec de l'import », conseil non affiché — défaut
produit à noter), trois retouches de surface. Publication toujours après les
sections 1 à 4 ; relecture owner de la page FR `.job` dès maintenant.

**D1 (documentation, PR #14 du site vitrine, `74d1591`) vérifié le 16/09 :
socle GO, publication NO-GO, lot D1-bis** (plan documentation, section
« Vérification du lot D1 »). Le montage Starlight tient (build et liens
rejoués en code 0, routes, langue, recherche, lien « Docs »). Le texte de
« Démarrer » porte cinq affirmations fausses ou inexactes — « machine
d'essorage », « Nos serveurs » calcule sur nos machines (faux en gratuit :
calcul navigateur, deux tôles), `.job` serveur tu, libellés de badges
inventés, phrase FR sans sens — et les deux captures montrent le compte de
développement du propriétaire (l'une n'est même pas utilisée, l'autre montre
une tôle vide sous une légende qui promet la pièce). Règle : compte local
neuf dédié aux captures, captures d'élément, aucune image orpheline ; menu
par section avant D2. Commit de charte `308eb08` dans la PR : au
propriétaire.

**J11-ter DÉPLOYÉ le 16/09 à `a22b7d5a` (app seule), contrôle vérificateur
conforme (plan UX §8)** : page de production au SHA complet, V0.9 / V0.9.0
affichés, « se neste » rendu, digest identique registre `:latest` =
`:a22b7d5a` = conteneur, aucun diff worker/moteur depuis `711f4b44`. Prod =
**V0.9.0**. Suite : recette propriétaire C1–C11 en production.

**J11-ter `bc5eca67` vérifié (16/09) : GO app seule** (plan UX §7). La vue
agrandie d'une fiche `.job` est réparée et REGARDÉE : amorce et disque de
perçage dessinés entiers (viewBox étendu), badge redevenu badge (64 px),
légende dans son bloc sans chevauchement à 1440 comme à 390 px ; cause racine
juste (`.then(() => import(…))` remplaçait le module résolu, `Promise.all`
corrige). Vitest 796/796 code 0, harnais `qa-job-leadview.mjs` 12/12 GO.
Un caractère corrigé par le vérificateur dans `CHANGELOG.md` (« nestE » ⇒
« neste ») : **le commit docs + ce caractère est le SHA à promouvoir**.
À la promotion : remettre les dates de `whatsNew.js` à la date du
déploiement. Résidus hors lot : plein écran de la vue (dessin plafonné à
280 px, bande vide), 429 sur les vignettes de résultats d'une longue liste
(famille du piège #43), hydratation `/home`.

**J11-bis `75758c4d` vérifié (16/09) : NO-GO étroit, lot J11-ter d'un seul
écran** (plan UX §5). Neuf points sur dix tiennent et sont mesurés : carte
groupée à 864 px avec quatre vignettes, page Nouveautés propre et en
français, badges posés, nom du projet au modal, version au pied de page,
compte de fichiers unifié, Support statique à 390 px, **le serveur honore le
cookie de langue** (`curl` FR ⇒ « Créez votre compte »). Le dixième, la vue
agrandie d'une fiche `.job`, est **cassé à l'écran** : légende imprimée
par-dessus le nom du dessin, badge en barre pleine largeur, et **aucune
amorce dessinée** malgré la légende — la capture committée par
l'implémenteur le montre, elle n'avait pas été regardée (il l'a dit
honnêtement : pas de vision d'image cette session). Choix laissé : réparer
ou revenir à la vue sobre. Reste « se nest » ⇒ « se neste ». Deux
désaccords d'hydratation ANTÉRIEURS sur `/home` nommés (salut calculé en UTC
côté serveur, ligne de marque rendue côté client) — hors lot.

**Règle ferme (propriétaire, 13/09)** : en production, NestorCut comprend un
`.job` SEUL, sans G-code. Le lecteur `.tap` est un outil de MESURE du poste
de développement, qui sert à écrire puis verrouiller les règles de SheetCam ;
le module d'enveloppe ne lit que les clés du `.job`. Aucune dépendance au
`.tap` ne doit entrer dans `app/`, `shared/` ni `workers/`.

## 6. Après la priorité 4 — ordre des chantiers (page du propriétaire)

| # | Chantier | Matière déjà mesurée, à reprendre |
|---|---|---|
| 5 | **Recoudre les contours ouverts** (couture) | 33 fichiers réels à segments pendants ; **704 entités attachées à aucune pièce sur 9 fichiers** (absentes des DXF de coupe, `PLAN-ECLATEMENT` §5.3) ; deux fichiers à « non-consecutive duplicate vertices » ; l'importeur serveur **non reproductible** sur un fichier pathologique (1365/1334/1334 tracés ouverts) ; inventaire GitHub d'abord (`ezdxf.edgeminer` / `edgesmith`) — décision propriétaire 10/09 |
| 6 | Un seul post-pass, dans le moteur | `MASTERPLAN` T3 point 2 |
| 7 | Bibliothèque de chutes + **conteneur polygonal** (prérequis commun) + idée « tôle sur photo » (référence connue, contour cliqué, homographie 4 coins) | registre du masterplan, 12/09 |
| 8… | UI états/a11y, rapport imprimable, benchmarks navigateur et tôle portrait, contraintes de tôle, amorces, T4-T5 | `RESTE-A-LIVRER-2026-09-10.html` |

Dette connue, non bloquante : le harnais 900 pièces se bloque après son
verdict dans le rendu three.js sur le poste du vérificateur (GPU stall) ;
« trois déposes successives dans un même projet » instables dans le harnais
`qa-e2e-advanced-import.mjs` (cas H, non élucidé) ; le panneau du coffre est
visible pour tous les comptes depuis le retrait du strip (conforme à
`STRATEGY.md`).

## 7. Les règles de la maison, en dix lignes

1. **GO avant déploiement**, toujours ; déploiement par SHA promu (§1).
2. **Mesure au repos** : aucun build ni coureur en parallèle quand on
   chronomètre ; `QA_OUT` hors OneDrive ; deux passages avant de conclure
   sur une répartition BPP (`CLAUDE.md`).
3. **Images à HEAD** avant tout banc (`assert_images_head.sh`), workers
   recréés (`docker compose up -d --force-recreate`), cache navigateur du
   wasm vidé (piège #14i).
4. **Fichiers réels privés** : `specs/import-corpus/` (153 DXF, noms de
   clients), `.testparts/` (DXF, `.job`, fichiers GPL) — **jamais nommés
   dans `docs/`** (identifiants = préfixe de sha256), jamais commités,
   copies anonymes pour les harnais.
5. **Une seule chaîne de code** : navigateur d'abord, miroir serveur
   ensuite, parité mesurée (`workers/geometry/parity`, coureurs sur les 238
   fichiers, `determinism_lock.py` natif ≡ wasm, tolérance 0).
6. **Rebuild du wasm dans la même PR** que tout changement Rust
   (piège #33b), goldens régénérés seulement avec la preuve que l'ancien
   était faux.
7. **Rapport par lot** dans le document du chantier, chiffres mesurés,
   non-faits explicites, contrôle négatif quand un verrou pourrait être vide.
8. **Commits** : français, conventional commits, petits, chemins explicites,
   pas de force-push sur `main`, pas de travail non sollicité d'autrui.
9. **Écriture en production hors déploiement** (comptes de test compris) :
   demander au propriétaire d'abord.
10. **Langues** : EN + FR dans `i18n.js`, rien en dur ; UI sans pilules
    (rayon 4 px), pages pleine largeur ; libellés visibles sur toute stat.

## 8. Outils et emplacements

- Coureurs d'import : `scripts/qa-import-wasm.mjs`, `scripts/qa-import-ezdxf.py`
  (dans l'image `nest2d-file-processing-worker:dev`, `MSYS_NO_PATHCONV=1`
  sous Git Bash) ; moteur × corpus : `scripts/qa-engine-corpus.mjs`.
- Harnais navigateur (Playwright, `npm i --no-save playwright`) :
  `scripts/qa-e2e-local-2sheets.mjs` (900 pièces, `QA_SPACE`, `QA_OUT`),
  `scripts/qa-e2e-advanced-import.mjs` (cas A-H), `scripts/qa-e2e-item-geometry.mjs`,
  `scripts/qa-derniere-tole.mjs`.
- Bancs moteur : `workers/nesting/bench/` (`determinism_lock.py`,
  `seed_demo_dirs.py BENCH_ASSERT=1`, `lock_last_sheet.py`, `oracle_parity.py`,
  `densities_corpus.py`, `assert_overflow_head.py` — adresse et accès du
  homelab dans le runbook privé `specs/infra/`).
- Compte de test local : `guillaume@local.dev` (`AGENTS.md` §4) ; la pile
  locale : `docker compose build && up -d`, app sur `localhost:7100`.
- Sorties de vérification du poste précédent (hors dépôt) : `~/qa-out/`.

## 9. Questions ouvertes pour le propriétaire

1. Recette SheetCam : verdict rendu le 13/09 (pièces et ordre OK ; amorce du
   trou qui coupe les éventails : NOT OK, traité en J4 par la morsure). **À
   rendre maintenant** : sur les deux `.job` remis le 13/09 au soir
   (`.testparts/VERIF-J4_*`), l'amorce du trou coupe-t-elle encore un
   éventail niché ? Et l'angle `-6.283` s'était-il affiché correctement ?
2. Rayon du disque de perçage par défaut (3 mm ?) et table
   `Start position → coin` : à confirmer sur la machine.
3. Miroirs `HRef`/`VRef` : un essai SheetCam sur une pièce non symétrique,
   un jour.
4. Rangement de l'idée « tôle sur photo » (après le conteneur polygonal).

**DÉPLOYÉ le 14/09 à 00 h — `9b227ec1a5a267cd7f831ffbd189babbc5d692e4`**
(étude §9.55). J4-ter et E3 dans la même promotion : build `34784870439`
vert, `promote-latest` `34785161622` vert, Hetzner `pull` + `up -d` (app,
worker nesting, worker fichiers recréés, site 200), homelab
`--force-recreate` sur les trois workers de débordement,
`assert_overflow_head.py` **OK**, jetons 28 des deux côtés. Corpus rejoué :
**neuf des dix fiches publiées identiques**, la dixième (T-F) bouge dans son
bruit propre — A/B sur l'image de référence `d890c92` à l'appui (88 et 90
contre 89 à HEAD) — donc `data/benchmarks.js` n'est pas réécrit.

**Reste ouvert pour le propriétaire** : la règle d'espacement (1 mm de
matière entre les bandes de kerf aujourd'hui, contre `4 × kerf + sécurité`
si l'on en veut deux kerfs — mon avis au §9.56 : garder la règle actuelle et
monter la sécurité), et la stabilité de la fiche T-F de la page publique.

**Suite, dans l'ordre de la consigne** : chantier « meilleur point de
départ » (choisir le point le plus loin des pièces nichées au lieu du point
lu), puis J5 (miroir serveur du `.job`), puis la couture des contours
(priorité 5).
