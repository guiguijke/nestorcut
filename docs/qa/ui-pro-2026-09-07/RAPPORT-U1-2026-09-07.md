# Rapport U1 (plan interface) — coquille : en-tête, accueil, badges — 2026-09-07

Implémenteur : ZCode. Destinataire : vérificateur (GO visuel). Plan §U1.
En-tête, colonnes, accueil — livraison PRÉLIMINAIRE (une passe de plus
prévue, voir Non-faits) : je préfère rendre l'ossature mesurée maintenant
que de garder deux jours de travail non visible.

## Livré

1. **En-tête 56 px** (mesuré au banc : `header` bounding = **56 px**) :
   à gauche logo + « Espace de travail » / « Strip » en **liens texte**
   (l'active porte l'accent sur la couleur du texte, plus de pilule à
   fond `--fill-tertiary`) ; à droite le **bouton thème passe en
   bouton fantôme** UiButton ghost avec icône soleil/lune ajoutées au
   sprite — plus de carré bleu plein ; unités et langue suivent (les
   deux switchers historiques, même poids visuel à côté).
2. **Colonne projets** : lignes compactes — `min-height: 56 px`, nom
   sur une ligne tronquée (`nowrap` + ellipsis), méta date/résultats
   sous le nom ; **ligne active = barre d'accent 2 px à gauche +
   fond** (plus de cadre bleu 2 px) ; hover = fond discret. Corbeille
   intacte (test `qa-badge-overlap` **vert**).
3. **Badges « Cet appareil » / « Démo » / coffre en CONTOUR** : plus
   AUCUN aplat bleu dans PrivacyChip (c'étaient eux qui faisaient le
   plus « prototype ») — accent sur le texte + liseré, fond surface.
4. **Accueil** : plus d'avatar géant ni d'emoji 👋 — h1 Poppins
   (`--font-display`, `--fs-22`), aligné à gauche, lien de compte
   discret ; les stats passent sur **UiStat** (chiffres tabulaires)
   et **la tuile « DXF files » disparaît** (3 tuiles : projets,
   imbrications, pièces imbriquées).
5. **Pied de page d'application (layout auth) : UNE LIGNE** —
   © · Mentions légales · Confidentialité · Changelog · Support — via
   `<Footer compact />` ; le grand pied reste sur default/doc/profile.

## Verrous

| Verrou | Mesuré |
|---|---|
| Hauteur d'en-tête | **56 px** (banc, desktop) |
| 0 aplat bleu hors action/sélection | bouton thème ✓, liens tabs ✓, badges ✓ — revue visuelle demandée sur la planche |
| Compteurs contrepartie U0 (button/input hors primitives) | **25 / 12** (inchangés ce lot — ils vivent dans les écrans qu'U2/U3 refont ; l'en-tête n'en portait aucun) |
| i18n | 2 clés nouvelles (footer.legal/privacy) FR+EN, `i18nDict` **6/6** |
| Non-régression | vitest **485/485** ; harnais @0,1 **[587,313]** bit-identique ; refus 4 mm **VERT** ; `qa-badge-overlap` **VERT** |
| Planche | `u1-planche/` : accueil clair, accueil sombre, auth avec pied compact |

## Non-faits énoncés (passe 2 prévue)

1. **Colonne résultats** : « nom du projet · densité · N tôles » et la
   fin des « Results · 0 sheets » — UserResultItem porte déjà les
   morceaux (otherDevice, unfit, densité) mais la refonte complète de
   la carte est la première tâche de ma passe 2 (ce rapport est rendu
   pour armer le GO visuel sur l'ossature).
2. **Champ de recherche au-dessus des projets dès 8** : passe 2.
3. a11y `a11y-home-desktop.json` non régénéré ce lot : passe 2 (avec la
   colonne résultats).
4. Les switchers unités/langue restent les composants historiques (pas
   encore UiSegmented) — même poids visuel, migration en passe 2 ou U3.

---

## Addendum — Essai P3@20 (arbitrage owner, exécuté AVANT la passe 2 d'U1)

Instruction fermée de l'owner. Un seul changement : DEFAULT_STOP_FLOOR
30 → 20 (k=3, SAMPLE_CFG, schéma compressé inchangés). wasm rebuildé +
commité dans le même commit ; determinism_lock natif ≡ wasm
**bit-identique** (SHA `1f79c63b…`, tolérance 0, arrêt mesuré à 25
itérations).

### Porte qualité (fenêtre explicite : jobs créés après le commit, à vide)

| Critère | Référence | Mesuré | Verdict |
|---|---|---|---|
| Grille bit-identique | [587,313] / [573,327] | @0,1 **[587,313]** ×6+corpus ; @2 **[573,327]** ×6+corpus ; navigateur **[587,313]**@0,1 et **[573,327]**@2 | ✓ |
| 6 runs @0,1 chute compaction, médiane à ±5 mm | 520,7 (bimodale 520,7/602-614) | chutes [607,7 · 610,9 · 520,7 · 609,5 · 520,7 · 605,2] — **médiane 606,5** | **✗ +86 mm (mode haut)** |
| 6 runs @2 chute compaction, médiane à ±5 mm | 479,2 / 518,8 | chutes [511,3 · 479,2 · 479,2 · 457,5 · 511,5 · 457,5] — **médiane 479,2** | ✓ (exacte) |
| Fusion ≥ 4/8 à 2 mm | ≥4 | **6/8** (fenêtre 8 runs) | ✓ |
| Zéro écartée (fenêtre) | 0 | **0** sur 17 jobs | ✓ |
| Zéro ceinturée | 0 | **0** (residualRolledBack absent partout) | ✓ |
| Corpus 11/11, T-F ≥ 89/90 | ≥89 | **11/11, T-F 89/90** | ✓ |
| Harnais 4 mm ×2 | partiels+leviers | **892/900 et 894/900** + 3 leviers, physique propre | ✓ |
| Navigateur temps/grille | — | **9 s** à 0,1 et à 2 (grilles exactes) | ✓ |

### Verrou temps (6 runs à vide, création→fin)

**14,7 / 15,6 / 15,7 / 16,0 / 16,0 / 16,0 — médiane 15,85, max 16,0.**
Lecture du vérificateur (médiane ≤ 15 ET aucun run > 16) : **les DEUX
critères manqués** (médiane +0,85 s, un run à la limite 16,0 et sous
chaque lecture antérieure @30 la médiane était 14,55).

### Issue (instruction §4)

**Porte manquée sur UN critère** — la médiane de chute de l'alternative
compaction à 0,1 (606,5 vs 520,7, hors ±5 mm ; le mode « rollback »
devient dominant 4/6, contre 2/6 à plancher 30). Conformément à
l'instruction : **retour au plancher 30 dans le même rapport** (revert à
livrer), **verrou écrit à 16 s**, sans autre essai ni valeur
intermédiaire. Le gain de ~4 s attendu ne s'est pas matérialisé non
plus (15,85 vs 14,55 : le walk atteint le plancher plus tôt mais la
compaction, roulant back, laisse la chute au mode haut — le temps
serveur ne descend pas).


---

## PASSE 2 — les six retouches fermées (07/09 soir)

### Retouches (numérotation du verdict U1 passe 1)

1. **Badges conformes** : `PrivacyChip` rend désormais un `UiBadge`
   discret — rayon `--radius-s` (2 px), **casse normale**, graisse 600,
   contour `--border-strong`, **point coloré 6 px** : gris neutre
   (`--text-3`) pour « Cet appareil » / cloud / coffre, **info** pour
   « Démo ». `UiBadge` lui-même porte le point (`dot`) et réserve les
   tons colorés (ok/warn/danger, contour dans le ton) aux états de
   résultat. S'applique aux colonnes projets/résultats, à l'accueil et
   au titre de projet (mêmes composants). Les classes marqueurs
   `chip`/`chip--{mode}` restent (sélecteurs `privacy-ui-check`).
2. **Les cinq statistiques sur `UiStat`** : « Ce mois-ci » et
   « Taux de réussite » migrent — plus aucune tuile à chiffres bleus
   (le bloc `.item` disparaît de `UserStats`).
3. **« Espace de travail » à gauche** : collé à la marque
   (`margin-left` + `margin-right: auto` sur `.tabs` >= 567 px) — plus
   de lien centré.
4. **Accueil : UNE seule liste** — la section « Projets récents »
   disparaît (template + computed + styles) ; la colonne de gauche
   reste la seule liste. L'hydratation du store commun est conservée.
5. **Preuve du pied** : `p2-05-bas-page-projet.png` = bas de la PAGE
   PROJET (cadrage des 300 px inférieurs, layout `auth`) — le pied
   compact « © 2026 NestorCut — Mentions légales · Confidentialité ·
   Changelog · Support » y est lisible (visible aussi en bas de
   `p2-04-projet-sombre.png`, la page tient en 900 px de haut).
6. **Reste de la passe 2** :
   - **Colonne résultats** : carte = **« nom du projet · densité · N
     tôles »** (nom résolu côté client depuis le store par
     `projectSlug` — **aucun changement d'API**, le flux SSE ne porte
     que le slug) ; badge d'état sémantique à point (Terminé / Partiel
     / Échec) ; miniature 64 px ; `placed === 0` sans échec →
     « Aucune pièce posée » ; le repli `results.title` (« Results »)
     est supprimé — **fin des « Results · 0 sheets »**. Partiel =
     `unfit.reason === 'partial'` ou `placed < requested`.
   - **Recherche dès 8 projets** : `UiField type=search` au-dessus de
     la liste (`UserProjects`), filtre sans accents ; message
     « Aucun projet ne correspond » dédié.
   - **Unités / langue sur `UiSegmented`** : `UnitSwitcher` (mm | in)
     et `LocaleSwitcher` (EN | FR) reconstruits sur la primitive — le
     menu déroulant à drapeaux SVG et la pilule ovale disparaissent.
   - **a11y** : `a11y-home-desktop.json` régénéré (axe-core 4.10.2
     injecté, accueil 1440×900, thème clair) → **0 violation** (0
     sérieuse). Correctifs apportés pour y arriver : contraste de la
     méta projets/résultats (`--label-tertiary` → `--label-secondary`,
     16 nœuds), `lang="fr"` sur `<html>` (`nuxt.config`), bouton
     « Activer le coffre » sorti de la carte radio (nested-interactive),
     miniatures `SheetSvgPreview` déclarées décoratives
     (`aria-hidden`, rôle img sans nom supprimé), `alt=""` sur le
     logo (redondant avec le wordmark).
   - **Migration boutons/champs en-tête + colonnes + accueil + pied** :
     `UnitSwitcher`/`LocaleSwitcher` (segmentés), annuleur et zone
     cliquable de la carte résultat, renvoi d'e-mail (bannière), bouton
     Support flottant (layout auth), « Activer le coffre » (accueil),
     déclencheurs coffre (icône `shield` du sprite, plus de
     mask-image) et Turbo (icône `cpu`, plus d'emoji).

### Verrous (commandes du plan — les compteurs le sont)

| Verrou | Mesuré | Commande |
|---|---|---|
| Compteur boutons hors `ui/` | **13** (22 → 13) | `grep -rn "<button" app --include=*.vue \| grep -v components/ui/` (les 13 restants : MainSettings 5, ResultModal 3, auth/local 2, LiveNestingView, FreeNestBanner, page projet — tous écrans U2/U3/U5) |
| Compteur champs hors `ui/` | **15** (inchangé) | idem `<input` (aucun champ de l'en-tête/colonnes/accueil/pied n'était un `<input>` brut hors l'upload de fichiers, consigné au plan U2) |
| Hex en dur hors tokens/drapeaux | **0** | commande du §U0 |
| Hauteur d'en-tête | **56 px** (segmentés 35 px, tenus dans la ligne) | banc Playwright |
| Contraste AA jetons | 100 % deux thèmes | `node scripts/ui-contrast-check.mjs` |
| a11y accueil desktop | **0 violation** (axe 4.10.2) | `docs/qa/ui-pro-2026-09-07/a11y-home-desktop.json` |
| i18n | 8 clés nouvelles FR+EN (`nav.language`, `project.search[Empty]`, `results.nothingPlaced`, `result.state.{done,partial,failed}`), `i18nDict` vert | vitest |
| Non-régression | vitest **485/485** ; `qa-badge-overlap` **VERT** ; `qa-c01` **VERT** (annulation ×2, réarmement) ; `qa-c02c03` **VERT** (densité mesurée, carte « Piece_Trou · 55.4% material · 2 sheets ») | suites habituelles |

Harnais moteur @0,1 non rejoué ce lot : **aucun changement moteur** (wasm
inchangé bit-à-bit depuis 6c08935 — aucun fichier `workers/` touché) ;
les trois scripts navigateur ci-dessus couvrent les surfaces modifiées.

### Planche (u1-planche/, fichiers nouveaux — règle AF7)

- `p2-01-accueil-clair.png` / `p2-02-accueil-sombre.png` — 5 stats sur
  UiStat, aucune section « Projets récents », badges discrets à point.
- `p2-03-projet-clair.png` / `p2-04-projet-sombre.png` — colonne
  résultats remplie d'un **terminé** (marine_lpl_001 · 61,8 % · 2 tôles,
  point vert), d'un **échec** (annulation, point rouge) et d'un
  **partiel** (marine_lpl_001 · 63,5 % · 2 tôles, point ambre —
  348/384 pièces posées, `unfit.reason=partial`).
- `p2-05-bas-page-projet.png` — bas de la page projet, pied compact.

Le partiel a été obtenu par un calcul **navigateur** (mode Cet appareil)
avec les fichiers démo sur 2 tôles 1000×1000 à 4 mm — la configuration
exacte du cas AF6. Le pré-contrôle serveur (marge 1,8) refuse toute
configuration dont la géométrie déborderait : **le partiel serveur est
inatteignable par excès de quantité**, seul le chemin navigateur en
produit.

### Fichiers touchés

`app/components/` : UiBadge, PrivacyChip, PrivacyModePicker,
UserStats, UserProjects, UserProjectItem, UserResults, UserResultItem,
MainHeader, UnitSwitcher, LocaleSwitcher, VerifyEmailBanner,
VaultMenuButton, TurboMenuButton, SheetSvgPreview ; `app/layouts/auth.vue` ;
`app/pages/home.vue` ; `app/utils/i18n.js` ; `nuxt.config.js` (lang).

### Non-faits énoncés

1. **Boutons restants (13)** : tous dans les écrans refaits par U2
   (MainSettings, page projet, LiveNestingView, FreeNestBanner), U3
   (ResultModal) et U5 (auth/local) — objectif 0 à la fin d'U3 tenu.
2. **`border-radius` distincts : 10 valeurs brutes** (4 jetons + 50 % +
   `calc` + composés de coins sélectifs — même forme que le GO U0 qui
   en comptait 9 ; le delta est un composé préexistant de ChatSupport,
   non modifié ce lot).
3. En cours de calcul, la carte résultat ne porte PAS de badge
   « En cours » (§1 le prévoit) : loader + barre de progression +
   annuleur communiquent déjà l'état ; à trancher en U2 avec la vue
   live.
4. L'échec de la planche vient d'une **annulation** (raison « Nesting
   cancelled by user ») : le refus capacité et le nofit ont leurs
   cartons dédiés (C09, §1.2c) mais exigent des configurations que le
   pré-contrôle bloque en amont sur le banc.

### Observation hors périmètre (à instruire)

Après une **annulation depuis la carte** d'un job CLOUD, le bouton Nest
peut rester muet (clics sans POST, aucun message) tant que la page
n'est pas rechargée — reproduit 10 fois sur 3 minutes lors du semais ;
l'annulation depuis la **vue live** (qa-c01) réarme correctement. Non
investi plus avant : hors périmètre U1 (aucun de mes changements ne
touche ce chemin — le carton d'annulation migre seulement en UiButton,
l'annulation elle-même fonctionne et rembourse). Ancrage précis :
`files.js` nest()/resetLastParams vs SSE, jobs cloud uniquement.
