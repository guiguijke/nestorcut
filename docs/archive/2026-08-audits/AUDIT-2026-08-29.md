# Audit — correctifs nesting & démo (sessions 2026-08-28 → 2026-08-29)

Document de passe d'audit : chaque correctif avec problème constaté, cause
racine, fichiers touchés, garde ajoutée et validation. Tout est en local
(branche de travail non commitée) — la prod n'a jamais été touchée.

Contexte corpus de test : `.testparts/` — `Piece_Trou` (carré 100×100, trou
r35, 6151,5 mm²) + `Piece_Fillx4` (quart-de-disque ~39,6×28, ~615,6 mm²,
4/trou en pinwheel). Projet démo : 24 pièces marines à encoches, 304
unités, 1500×3000 ×3, espacement 2, calcul navigateur (« THIS DEVICE »).

---

## A. Pass structurel « grille canonique » (SPP)

### A.1 — Bug « demande exacte » : la grille ne se déclenchait jamais

- **Constat** (user, calm-newton 50 Trou + 200 Fillx4) : « le grid n'a pas
  le résultat attendu avec les rectangles en cascade ».
- **Cause racine** : la pré-passe « trous d'abord » (J-085/D-MOT-16)
  extrait TOUTES les petites pièces vers les trous quand la demande est
  exacte (200 = 50 trous × 4) → l'instance de solve réduite ne contient
  plus qu'UNE classe → `detect_structural_case` (exige 2 classes) ne se
  déclenche jamais → aucune alternative grille n'existe. Complément : le
  post-pass `applyHoleFill` aurait de toute façon TÉLÉPORTÉ les fans des
  zones vers les trous vides.
- **Fix** : le pass structurel consomme la **vue ORIGINALE à 2 classes**
  (quantités complètes depuis `payload.parts` + rotations : instance si la
  classe y vit, sinon union des `ringRotations` de la meta pour le filler).
  Nouvel ordre de remplissage : **A → C → trous des hôtes → B** (zones
  internes d'abord = silhouette rectangulaire pleine ; trous = 2e
  réservoir via `expandMeta`/`expand_meta` avec slots recalculés ; B en
  dernier = seule zone qui étend l'axe objectif).
- **Auto-suffisance** : l'alternative structurelle porte les ids
  D'ORIGINE et remplit ses trous elle-même → la finalisation SAUTE pour
  CETTE alternative le remap idMap (corromprait les ids), l'expansion
  meta (doublerait les fillers) et `applyHoleFill` (vidait les zones).
  Drapeaux : `structural: true` + `selfContained: true` (JS) /
  `self_contained` (Python).
- **Fichiers** : `app/composables/localJobPrivate.js`
  (`buildGridAlternative` — vue originale, holePlan, garde anti-perte),
  `app/composables/structureClient.js`, `app/composables/localBridge.js`
  (skip expansions), `workers/nesting/core/structure.py`
  (`build_structural_layout(hole_plan=…)`, `small_lattice`),
  `workers/nesting/core/main.py` (intégration + skips des 2 boucles
  de post-pass).
- **Validation** : e2e THIS DEVICE 50/200 → grille 300,4 mm (2 colonnes
  pleines + 12 reste), zones A=73/C=20 fans, trous=107, B=0, silhouette
  pleine 300,4×2000, bande droite 0,2 % d'encre ; banc worker : `grid`
  rang 0, `overlapFree`/`spacingOk` vrais (250 pièces = limite du garde),
  250/250.

### A.2 — Rectangles successifs (~450 mm) par zone

- **Constat** : zones internes remplies à 38 % (A) et 27 % (C) — bandes
  creuses, pas de « rectangles pleins ».
- **Cause** : banc isolé — le solve plafonne à ~57-60 % de densité sur une
  bande 100 mm ENTIÈRE, indépendamment de la taille, mais tient ~60 %
  PAR TRONÇON de 300-500 mm (49 fans/500 mm ×3 = 147 vs 89 d'un coup).
- **Fix** : `_zone_steps`/`zoneSteps` découpe chaque zone le long de son
  grand axe en pas de `ZONE_STEP_MM = 450` (dernier tronçon ≥150 mm
  fusionné), chaque tronçon rempli ENTIER avant le suivant. Budget ENTIER
  par tronçon (le plateau arrête les runs convergés), abandon sous
  `ZONE_STEP_BREAK = 0,45` du demandé (0,60 tuait des tronçons au
  plafond réel ~0,57).
- **Fichiers** : `structure.py`, `structureClient.js`.
- **Validation** : worker 100/800 : zone A 89 → 120 fans uniformes
  (~10/100 mm sur toute la bande), étendue 684 → 673 mm ; navigateur :
  profil A = 8-12 fans sur chacune des 15 tranches de 100 mm (avant :
  entassées en bas, haut vide).

### A.3 — Lattice analytique des petites pièces (« compression finale »)

- **Constat** (user, captures annotées) : « adaptatif, compression finale
  en direction de l'optimisation » — les lignes 570/655 = largeurs
  physiques cibles des blocs fans, inaccessibles au moteur (plafond 57 %).
- **Fix** : `small_lattice` (Python/shapely+STRtree) et `smallLattice`
  (JS/géométrie pure : `ringDist` sommets↔arêtes, voisins |Δi|,|Δj|≤2) :
  colonnes entrelacées rot0/rot180 (le quart-de-disque s'interpénètre en
  zigzag — pointes fines passant les flancs). Paramètres empiriquement
  validés : `px = W/2 + space`, `py = 1,3554·H`, `dy = −0,378·py`,
  orientation `(i+j)%2`, pièces CENTROÏDE-normalisées (l'ancrage bbox
  casse la phase).
- **Marge de sécurité** : le layout se calcule sur les anneaux SIMPLIFIÉS
  (`NEST_SIMPLIFY_MM`, défaut 0,05) mais le DXF exporté copie les courbes
  BRUTES → validation à `space + 2×LATTICE_SIMPLIFY_MM` sur l'anneau
  simplifié (recalibré sur ring brut : min-dist 0,2 mesuré).
- **Tout-ou-rien par zone** : un top-up moteur dans la même zone
  ÉCRASERAIT le lattice (le solve ne voit pas les pièces déjà posées —
  300 fans dans une zone qui en tient 231, mesuré). Le surplus va aux
  zones suivantes/trous/B.
- **Fichiers** : `structure.py`, `structureClient.js`.
- **Validation** : 100/800 −X worker : grille 67,3 % vs moteur 65,0-65,4 ;
  navigateur : 67,9 % vs 65,6 (A lattice 158 + C 36 + trous 400 + B
  moteur 205 — le lattice vertical perd en bande large : 51 % vs 81 % au
  moteur → B reste au moteur). Vérification exhaustive 900 pièces
  (`bench/verify_grid.py`) : lattice 0 conflit.

### A.4 — Bug JS : placements de la zone B jamais poussés

- **Constat** : 689/900 pièces livrées en rang 0 lors d'un run navigateur.
- **Cause** : le bloc B de `buildStructuralLayout` résolvait puis
  comptait mais oubliait `placements.push(...got)` (l'édition avait perdu
  la ligne ; Python avait bien `placements.extend(got)`).
- **Fix** : push restauré + **garde anti-perte** dans
  `buildGridAlternative` : `placed_items.length !== totalRequested →
  return null` (miroir du part-loss guard serveur `_finalize_alternative`).
- **Fichiers** : `structureClient.js`, `localJobPrivate.js`.

### A.5 — Densité de la grille honnête

- **Constat** : densité grille 1,037 (>1) puis stats trompeuses.
- **Cause** : aire des trous comptée comme matière ; et calcul sur les
  quantités de l'instance réduite.
- **Fix** : densité = aire MATÉRIAU (outer − trous) / bande, calculée sur
  la vue originale. 50/200 → 0,717 (cohérente).
- **Fichiers** : `localJobPrivate.js`, `main.py`.

---

## B. Bugs du projet démo (BPP multi-tôles)

### B.1 — Test « une seule tôle » sur l'aire NETTE → 3 tôles écrasées en 1

- **Constat** (user) : « le projet de démo est buggé » ; résultat = 1 tôle
  au lieu de 3, ~440 chevauchements réels, pièces hors tôle.
- **Cause racine** : le garde `is_spp` (« tout tient sur une tôle »)
  comparait `total_part_area` = aire **nette** (trous déduits ; démo :
  3,39 m² ≤ 80 % de 4,5) alors que le placement occupe l'aire
  **enveloppe** (4,19 m², ratio 0,93) → mode bande (SPP) forcé à tort
  avec 304 pièces impossibles → chevauchements livrés (les contrôles
  pairwise sont skipés >250 pièces/tôle, garde préexistant).
- **Fix** : `total_outer_area` (anneaux externes) pour le test ;
  `total_part_area` conservé pour densité/journalisation.
- **Fichiers** : `workers/nesting/core/main.py` (~543, ~704),
  `app/composables/localPayloadBuilder.js` (~623, ~641).
- **Validation** : worker journalise `problem_type: bpp`,
  `total_outer_area: 4190149` (ratio 0,93) ; démo → 2 tôles
  (278+26 puis 260+44), 304/304, 0 pièce hors tôle.

### B.2 — Limiteur /api/files 30/min vs 24 géométries/page → 429 en boucle

- **Constat** : 128 erreurs console 429 sur
  `/api/files/project/geometry/demo-marine-lpl-0XX.dxf`, thumbnails
  morts, retries en boucle.
- **Cause** : la page projet consomme UNE géométrie d'affichage PAR
  FICHIER (24 pour la démo) ; le middleware anti-brute-force
  (`5_files_ratelimit.js`, 30 req/min/user sur `/api/files/**`) saturait
  dès le 2e chargement ou un calcul dans la même minute.
- **Fix** : budget séparé 180/min pour `/api/files/project/geometry/`
  (polygones d'affichage, pas des bytes de téléchargement) ; clé de
  compteur suffixée `:geometry`.
- **Fichiers** : `server/middleware/5_files_ratelimit.js`.
- **Validation** : rechargement démo → 0×429.

### B.3 — Champion appliqué au BPP : frame live livrée comme résultat

- **Constat** : la cause directe des 440 chevauchements — le résultat
  livré était une frame EN COURS d'optimisation.
- **Cause** : `preferChampion`/`settleFromChampion` (localPool,
  conçus pour figer un layout SPP convergé via métriques bande)
  remplaçaient le rang 0 du merge moteur par une frame live en BPP
  (métriques nulles → comparaison sans sens).
- **Fix** : `if (!pool.isSpp) return alternatives` (preferChampion) +
  `!pool.isSpp` dans le garde de settleFromChampion. En BPP, le merge
  moteur reste la seule source.
- **Fichiers** : `app/composables/localPool.js`.
- **Signature du symptôme** : layoutCount 1 + usedSheetShare ~1,0 +
  densité nulle sur un job multi-tôles.

### B.4 — Vue live BPP figée (« 1 maj et c'est tout »)

- **Constat** (user) : la vue live fait une mise à jour puis rien
  (instrumentation : 4 updates DOM en 132 s).
- **Causes (2, en cascade)** :
  1. **Rust** : le BPP n'émettait une frame `layout` QUE sur amélioration
     de l'incumbent — fréquent pendant la descente initiale puis
     quasi jamais pendant le plateau (minutes).
  2. **Vue** : `isBetter` (LiveNestingView) ne comparait pas le
     `remnant` (le seul champ qui progresse à bins égaux pendant le
     plateau) et ne remplaçait jamais un champion à qualité égale.
- **Fix** :
  - Rust : le heartbeat SA (1 Hz) embarque le snapshot de l'incumbent —
    `on_heartbeat(usize, &Cost, &BPSolution)`, `layout_event()` factoring
    dans `bpp/mod.rs` ; **wasm rebuildé dans la même PR**
    (bindgen --target web + wasm-opt -O3, hash navigateur vérifié = local).
  - Vue : comparaison `remnant` après `bins` ; à égalité PARFAITE en BPP
    la frame fraîche remplace le champion (rotation entre incumbents des
    walks) ; SPP garde l'incumbent (stabilité du corridor phase 2).
- **Fichiers** : `workers/nesting/engine/crates/nest-engine/src/bpp/sa.rs`,
  `.../bpp/mod.rs`, `app/components/LiveNestingView.vue`.
- **Validation** : ~50 frames/90 s au niveau Worker (hook) ; 867 offres /
  13 changements de layout visibles en 110 s + compteurs animés.
- **Piège cache** : `nest_wasm_bg.wasm` garde le MÊME nom — vider le cache
  HTTP navigateur après rebuild (fetch `cache:'reload'` ou Ctrl+Shift+R),
  sinon l'ancien tourne encore (vérif par hash SHA-256 servi vs local).

---

## C. UX & orchestration locale

- **Étapes zones cumulées** : libellé `grid: step {step}/{steps} · zone
  {zone} ({attempt}/{attempts})` (EN+FR, piège apostrophe i18n) — fixe
  « on était sur C puis ça repasse à B, on ne sait pas où on en est ».
  `structureClient.js` (zoneSolve porte step/steps), `app/utils/i18n.js`,
  `app/pages/project/[slug].vue`.
- **Robustesse navigation** (2026-08-28) : `localSolverRegistry.js` —
  registre module singleton (solves app-wide, isolation par projet),
  `ensureJob` idempotent (queued/running/done), file capée par tier,
  anti-rejoue au refresh (`getLocalResult`), `cancelJob` → POST /cancel +
  `cancelPool` par PRÉFIXE (`${jobSlug}-…` tue les pools de zones),
  sentinelle `ZONE_CANCELLED` → `{ok:false, error:'cancelled'}` (jamais
  local-fail). Tests : `app/tests/localSolverRegistry.test.js` (6).
- **Parallélisme tier** : `MAX_PARALLEL_NESTS = {free:1, standard:1,
  privacy:3}` serveur (`server/utils/entitlement.js`), 409
  `concurrent_limit` à l'enqueue (compte pending/processing/awaiting_local),
  file client + libellé « en attente d'un slot libre ».
- **Stats locales** : `usedSheetShareOf` (bbox réelle/tôle, rotation de
  l'anneau externe par transform) pour chaque alternative + stratégie
  `'grid'` ; densité grille honnête (cf. A.5).

---

## D. Engine (précédent, 2026-08-28 — pour mémoire d'audit)

- **Champion phase-2 SPP** : `MapBack{corridor, orig_height}` (progress.rs)
  — les frames phase 2 portent strip_width/used_height DANS LE REPÈRE
  TÔLE d'origine ; `liveBetter` (localPool) avec fenêtre d'égalité
  1 mm + tie-break used_height. Sans ça : champion aveugle, layout figé
  en phase 1.
- **P4 biais d'éjection par aire** : `CollisionTracker.container_bias`
  (aire/médiane)^β clamp [0.25,4] sur la perte conteneur PONDÉRÉE ;
  plombé par `NEST_EJECT_BIAS` (défaut 0 = OFF). Faisabilité et poids
  GLS purs. Verrou Rust `container_bias_scales_with_area`.

---

## E. Tests & vérifications

| Suite | Résultat |
|---|---|
| `npx vitest run` | 321 passés (27 fichiers) |
| `cargo test --release -p nest-engine` | 59 + 2 passés, 1 ignoré |
| pytest `workers/nesting/tests/` | 103 passés, 1 skip (integration_holes : fixtures absentes en local, préexistant) |
| pytest structure | 33 (dont `TestHolePlan` ×3, `TestSmallLattice` ×2, `TestObjectiveY`) |
| verify_grid.py (900 pièces, shapely) | lattice 0 conflit ; 13 contacts marginaux 0,01-0,1 mm² dans la bande B moteur = slack numérique du sous-solve, même ordre que l'écart simplifié/brut du moteur lui-même (0,0049 mm mesuré sur l'alt moteur) |
| e2e démo (THIS DEVICE) | 2 tôles, 304/304, 0 hors tôle, 0×429, vue live animée |

## F. Points ouverts connus (à trancher, pas des bugs bloquants)

1. ~~`applyHoleFill` s'exécute même quand `fillHoles=false`~~ — **CORRIGÉ
   2026-08-29 (passe d'audit, §J.6)** : le serveur gated déjà via
   `has_holes` (main.py — le constat initial était inexact pour lui) ;
   `localBridge.js` gate désormais ses deux post-pass sur
   `payload.fillHoles` (exposé par `buildLocalPayload`, défaut ON).
2. Contacts marginaux bande B (cf. E) — même ordre de grandeur que le
   comportement moteur préexistant ; une passe de respiration +0,02 mm
   des sous-solves zone suffirait.
3. `VERIFY_MAX_PARTS_PER_SHEET = 250` : contrôles pairwise skipés au-delà
   (garde O(n²) préexistant) — envisager un STRtree côté worker pour
   relever la limite.
4. Décisions produit en attente : `NEST_EJECT_BIAS` par défaut, grille en
   rang 1 par défaut, parallélisme Unlimited, budget temps du pass
   structurel dans le budget job, `determinism_lock.py` avant déploiement.
5. Compteurs de diagnostic `window.__champOffers`/`__champSwaps`
   (LiveNestingView) — inoffensifs, à retirer avant un éventuel déploiement.

## G. Scripts de banc ajoutés (non commités)

- `bench/seed_exact_fit.py` — variante paramétrable de seed_user_repro
  (`BENCH_HOSTS`, `BENCH_FILLERS`, `BENCH_DIRS`).
- `bench/verify_grid.py` — vérification physique exhaustive d'un layout
  livré depuis le SVG GridFS (reconstruction shapely, STRtree ; usage :
  `python verify_grid.py <slug>` + env `STRAT=left|grid`).
- Supprimés après usage : test_zone_density, search/tune/diag lattice.

## H. Note déploiement

Rien n'est commité. Découpage de commits proposé : (1) moteur Rust
(heartbeat BPP + P4 + MapBack), (2) pass structurel Python, (3) pass
structurel JS + registry + i18n, (4) correctifs SPP/BPP + rate limiter,
(5) bench. Avant tout déploiement : rejouer `determinism_lock.py`
(natif-1T vs wasm-1T), vider les caches navigateur (wasm même nom de
fichier), et mettre à jour les comptes de tests de la section 5
d'AGENTS.md si nécessaire.
## I. Inventaire Git pour la passe d'audit (79 fichiers, rien commité)

**Modifiés (code)** :
- Pass structurel : `workers/nesting/core/{structure.py*(nouveau), main.py,
  engine.py, nesting_input_builder.py}` ;
  `app/composables/{structureClient.js*(nouveau), localJobPrivate.js,
  localBridge.js, localPayloadBuilder.js}` ;
  `workers/nesting/tests/test_structure.py*(nouveau)` ;
  `app/tests/{structureClient.test.js*(nouveau), localBridge.test.js,
  localPool.test.js}`.
- Engine Rust (2026-08-28 + 29) : `nest-engine/src/{bpp/{sa.rs, mod.rs,
  constructive.rs}, progress.rs, spp.rs, config.rs}`,
  `sparrow/src/{quantify/tracker.rs, optimizer/separator.rs, config.rs}` ;
  `public/engine/nest_wasm_bg.wasm` (rebuildé, hash vérifié).
- Démo/BPP : `server/middleware/5_files_ratelimit.js` ;
  `app/components/LiveNestingView.vue` ; `app/composables/localPool.js`.
- Robustesse/tier : `app/composables/localSolverRegistry.js*(nouveau)` ;
  `app/tests/localSolverRegistry.test.js*(nouveau)` ;
  `server/utils/entitlement.js`, `server/api/project/[slug]/nest.post.js`,
  `server/tests/entitlement.test.js`, `app/utils/entitlementUi.js`,
  `app/pages/project/[slug].vue`, `app/utils/i18n.js`.
- Docs : `AGENTS.md` (pièges 14h-i, 39-45 + comptes tests), `README.md`,
  `docs/archive/2026-08-audits/LOCAL-MODE-PR5.md` (amendement), `docs/STRATEGY.md` (parallélisme
  Free), `data/changelog.js` (entrée 2026-08-29).

**Nouveaux scripts bench** : `seed_exact_fit.py`, `verify_grid.py`
(+ `search_fan_lattice.py` = recherche du lattice, conservé pour mémoire).
Rendus de validation dans `bench/out/*.png` et `bench/out_grid_*.svg`.

**Non concernés** : `.qa-pw/`, `.testparts/` (corpus de test local).

## J. Correctifs de la passe d'audit code (2026-08-29, 3e session)

Audits en profondeur Python+Rust (3 passes, contrevérification empirique
contre ezdxf 1.4.4 du venv). Correctifs appliqués ci-dessous ; les
correctifs du workspace `workers/geometry` (ordre INSERT R·S, degré SPLINE
négatif, codes entiers dxf_writer) sont DIFFÉRÉS : chemin navigateur
uniquement, chacun exige le rebuild du bundle wasm géométrie + l'extension
du corpus de parité (INSERT échelle non uniforme + rotation, miroir,
reparse closed/couleur) — voir §I et le rapport d'audit.

### J.1 — Frames live BPP en repère INTERNE (violation 14g, MAJEUR)
- **Constat** : `layout_event` (bpp/mod.rs) sérialisait `pi.d_transf` nu —
  jagua centre chaque pièce au centroïde à l'import
  (`pre_transform`), donc toute pièce non centrée était affichée décalée
  de son centroïde pendant la recherche BPP (le SPP compose via
  `emit_layout`, progress.rs ; l'export final compose via
  `export_layout_snapshot` — seul le live était faux). Défaut préexistant,
  diffusé à 1 Hz par le heartbeat de B.4 (invisible en vérif physique,
  qui ne porte que l'exporté).
- **Fix** : `layout_event` compose `int_to_ext_transformation(d_transf,
  pre_transform)` (signature + instance en paramètre, 2 call sites).
- **Fichiers** : `nest-engine/src/bpp/mod.rs`.
- **Validation** : cargo test + rebuild wasm (hash re-vérifié) +
  `determinism_lock` — cf. §K.

### J.2 — Annulation avalée par le pass structurel
- **Constat** : `EngineCancelled` (sous-classe d'`Exception`, pas
  d'`EngineError`) levé par les sous-solves de zones était attrapé par le
  `except Exception` global du pass grille → job annulé livré + facturé.
  La fenêtre s'était allongée avec A.2 (zones A→C→trous→B en tronçons).
- **Fix** : `except EngineCancelled: raise` avant le `except Exception`
  (nesting/core/main.py) — la finalisation cancelled+refund du solve
  principal s'applique désormais aussi pendant le pass.

### J.3 — Garde anti-panique #2b sur l'aire NETTE
- **Constat** : la garde « strip déflaté » (main.py + miroir
  localPayloadBuilder.js) comparait `total_part_area` (net) alors que
  jagua initialise la bande sur la géométrie réellement posée (≤ aire
  enveloppe) — sous-déclenchement sur pièces très trouées.
- **Fix** : `total_outer_area`/`totalOuterArea` des deux côtés (B.1 avait
  corrigé `is_spp`, pas cette garde-là).

### J.4 — `read_dxf` → None non vérifié (×3 workers)
- **Fix** : gardes + messages actionnables dans fileprocessing
  `_make_dxf_copy`, stripfileprocessing (le dépaquetage explosait en
  `TypeError` avant tout garde — restructuré), nesting
  `get_entities_from_dxf_file` (None empoisonnait aussi le cache doc).

### J.5 — Conversion HATCH non bornée + dépaquetage erroné (×2 copies)
- **Constat** : (a) un HATCH à motif hostile matérialisait des millions de
  segments en RAM (contournement de la limite d'entités → OOM → job figé,
  cf. l'absence de requeue des jobs `processing`) ; (b) `hatch_entity`
  (ezdxf 1.4.4) yield des PAIRES de Vec3 — le `line.start` existant
  levait AttributeError au premier segment de tout HATCH à motif (le
  `raise e` tuait l'import ; passé inaperçu car les HATCH de découpe sont
  presque toujours solid-fill → 0 segment).
- **Fix** : consommation paresseuse bornée à 20 000 segments/HATCH
  (au-delà : HATCH gardé tel quel + warning), dépaquetage correct
  `(start, end)`. fileprocessing + stripfileprocessing.

### J.6 — worker_loop : retries bornés, SIGINT, on_success isolé
- **Fix (worker_common/worker_loop.py)** : (a) re-file falsy compté
  (`processAttempts`, 3 essais max puis erreur actionnable) + backoff
  `idle_sleep` — fin de la boucle chaude possible ; (b) SIGINT traité
  comme SIGTERM (reset du job, plus de `processing` orphelin sur Ctrl+C
  dev) ; (c) `on_success` isolé après l'écriture `done` — un échec Mongo
  transitoire ne requalifie plus un job réussi en error+refund ; (d)
  re-vérification de statut robuste au doc supprimé (`or {}`).

### J.7 — Dépendances Python
- **Retraits** : Pillow / matplotlib / numpy des requirements de
  production (importés uniquement par bench/debug ; Pillow 10.4.0 portait
  34 CVEs OSV, images Docker allégées d'autant).
- **Bumps** (pré-validés : la suite pytest locale a tourné sur ces
  versions exactes) : shapely 2.0.7→2.1.2, pymongo 4.14.1→4.17.0,
  python-dotenv 1.1.1→1.2.3 (2 CVEs), `cryptography>=44,<51` (borné, was
  non épinglé). ezdxf 1.4.4 / svgelements 1.9.6 / spyrrow 0.9.0 déjà
  dernières.

## K. Validation de la passe J (2026-08-29)

| Suite | Résultat |
|---|---|
| `npx vitest run` | 321 passés (27 fichiers) |
| pytest `workers/nesting/tests/` | 104 passés, 3 sautés |
| pytest `workers/common/tests/` | 48 passés |
| pytest `workers/fileprocessing/tests/` | 33 passés, 2 sautés |
| `cargo test --release` (engine, cargo 1.94 local) | 60 + 2 + 3 + 1 passés, 1 ignoré — dont le NOUVEAU verrou `bpp_live_frame_matches_final_export_off_center` (pièce non centrée : frame live ≡ export, tolérance arrondis d'impression ; sans J.1 il échoue d'un décalage centroïde) |
| rebuild wasm `nest_wasm_bg.wasm` | fait (SHA-256 `b7157da0…`, glue `nest_wasm.js` inchangée — API stable) |
| `determinism_lock.py` natif vs wasm | **OK — bit-identical** (SHA-256 égaux, tolérance 0) |

## L. Retouche de livraison (relecture post-déploiement, 2026-08-29 soir)

La relecture des correctifs J sous l'œil de l'audit a relevé et corrigé
trois points avant le 2e déploiement :

### L.1 — J.2 incomplet : la re-levée brute d'EngineCancelled finissait en « error »
- **Constat** : `except EngineCancelled: raise` (J.2) propage une
  `EngineCancelled`, mais le worker_loop ne reconnaît que `JobCancelled`
  (`cancelled_exception`) → branche erreur générique : statut `error` +
  message vide au lieu de `cancelled`, refund OK mais UX fausse.
- **Fix** : helper `_finalize_cancelled()` partagé (statut cancelled +
  $unset des champs éphémères, exactement le handler du solve principal) ;
  le pass structurel l'appelle puis lève `JobCancelled(slug)`.

### L.2 — Piège 14i désormais côté UTILISATEURS : fetch wasm sans revalidation
- **Constat** : le rebuild du wasm (J.1) garde le même nom de fichier —
  `init()` nu fait un fetch avec cache HTTP par défaut : les navigateurs
  ayant déjà chargé l'ancien bundle continuent de faire tourner le moteur
  AVEC le bug de frame BPP.
- **Fix** : `engine.worker.js` passe une `Request(..., {cache:'reload'})`
  aux deux sites `init()` — revalidation à chaque boot de worker (304
  quasi gratuit quand inchangé). Résiduel documenté : l'edge CDN peut
  encore servir sa copie ~2 h (TTL .wasm) — un cycle court, acceptable ;
  un versionnage par query-string (gitSha) reste le fix complet si le
  piège revient.

### L.3 — Comptes de tests AGENTS.md §5 recalés
- cargo `-p nest-engine` : 61 → **66 + 1 ignore** (les 5 tests du verrou
  `bpp_live_frame_matches_final_export_off_center` + recounts) ; pytest
  nesting 103 → **104**, common 46 → **48**, fileprocessing 32 → **33**.

### État de livraison (remplace « Rien n'est commité » du §H)
- Commits `c00cfda..c4d7749` poussés sur `main` (découpage §H respecté,
  J.1-J.7 répartis dedans) + présent commit de retouche.
- CI « Build and publish Docker images » verte (tests natifs Linux +
  6 images ghcr), serveur Hetzner `pull && up -d` : app 200, 5 workers
  logs propres, `determinism_lock` bit-identical re-vérifié avant push.

## M. QA production (2026-08-30, compte QA dédié — identifiant au runbook privé)

Tests réels sur app.nestorcut.com après déploiement. Trois blocages
découverts, corrigés et redéployés dans la foulée (commit a0e2a19) :

### M.1 — MAJEUR : calcul local auto-saturé par le rate limiter (429 en boucle)
- **Constat** : démo (24 fichiers) en mode THIS DEVICE → 44× HTTP 429 sur
  `/api/files/project/dxf/demo-marine-lpl-0XX.dxf` (2 par fichier, retry
  boucle), finalisation coincée en `awaiting_local`, vue live figée sur le
  dernier frame.
- **Cause** : le pipeline local télécharge les bytes DXF bruts pour l'export
  client ; B.2 n'avait isolé que `/geometry` — les téléchargements
  partageaient le 30/min anti-brute-force.
- **Fix** : budget authentifié séparé (180/min, clé `:project`) pour les
  trois préfixes `project/{geometry,dxf,svg}`.

### M.2 — MAJEUR : starvation du pool compute par les tiers > pool
- **Constat** : démo « ×8 Pro » (et tout job d'un compte Pro) → job
  `pending` à vie ; la préparation locale ne démarre jamais → l'UI reste
  « Calcul en cours… » indéfiniment.
- **Cause** : `acquire_tokens` exige `used + cost ≤ total` ; Pro = 8 vcores
  > pool 4 (serveur 2 vCPU, NEST_COMPUTE_TOKENS=4) → jamais acquirable.
  La démo PROMET d'essayer le parallélisme Pro — c'est exactement ce qui
  gèle.
- **Fix** : le coût est borné au pool dans `acquire_tokens` (le job prend
  la machine entière plutôt que de mourir de faim) ; et les jobs
  `computeLocation=local` ne coûtent plus que 1 token (le worker ne fait
  que construire le payload — le navigateur solve).

### M.3 — MINEUR : registre local bloqué après annulation d'un job jamais préparé
- **Constat** : après « Annuler » sur un job local coincé côté serveur, le
  bouton « Imbriquer » devient MUET (aucun POST) jusqu'au rechargement de
  la page.
- **Cause** : `runLocalJobPrivate` attend `local-payload` sans timeout ;
  le registre (running=1) refuse la soumission suivante ; l'annulation ne
  rompt pas l'await serveur.
- **État** : la cause racine disparaît avec M.2 (le worker claim toujours).
  Durcissement client à prévoir : timeout sur le fetch du payload +
  message visible quand une soumission est refusée par le registre.

Validations re-jouées après redéploiement : cf. §N.

## N. Résultats QA production après redéploiement (2026-08-30, §M corrigés)

Compte QA dédié (grant Pro/privacy pour les tests ; identifiant dans le runbook privé) :

| Scénario | Résultat |
|---|---|
| Démo THIS DEVICE **×8 Pro** (le scénario qui gelait avant §M.2) | **done, 304/304, 2 tôles, densité 0,375** — conforme au banc (B.1 : 3,39 m² nettes, 2 tôles). Modal complet : « Toutes les pièces placées », 50,8 % utilisé, chute réutilisable 1197,9×3000, matière à prévoir 2 tôles, tableau par tôle. |
| **100 Trou + 800 Fillx4**, serveur, tier Pro (clamped 4/4 tokens), 1000×2000×1, space 2, −X | **done, 900/900, SPP correct** (aire enveloppe 1,49 M ≤ 1,6 M) ; **alt rang 0 = grille structurelle** (share 73,75 % vs moteur 73,54 % — la grille gagne sur l'axe objectif, conforme au comportement livré) ; rapport matière net 55,4 % ; contrôles pairwise sautés (900 > 250, garde documentée §F.3). |
| Rate limiter (M.1) | **0× 429** sur les deux scénarios (44× avant le fix) |
| Annulation job pending (API + UI) | statut `cancelled` propre + refund, deux chemins |
| Santé post-déploiement | 0 erreur sur les 4 workers, 0 429/5xx app, tous conteneurs up ; scorie nettoyée : job `awaiting_local` orphelin du pentest 21/08 (navigateur mort) finalisé en `error` actionnable |
| Déterminisme prod | wasm servi = wasm commit (SHA-256 `b7157da0…`, piège 14i vérifié en prod) |

**Reste ouvert (documenté, non bloquant)** : §M.3 durcissement client (timeout
`local-payload` + feedback quand le registre refuse une soumission) ; le
pool 4 tokens reste petit pour le tier Pro sur le 2 vCPU — le clamp §M.2
fait que Pro = toute la machine (correct mais sérialisant : un job Pro
bloque les autres le temps de son run ; voir la question infrastructure
« compute à la maison »).

## O. Cloisonnement des projets (2026-08-30, livré + vérifié en prod)

Constat utilisateur : « si je change de projet pendant un nesting tout est
reset — sentiment de produit non achevé ».

- **Cause** : `files.js` est un singleton module-level (un seul état pour
  tous les projets). Au changement de projet, `setProjectFiles` repartait
  avec `countBySlug` vide (quantités → défaut) et les `params` (tôles,
  espacement, sens) FUISAIENT d'un projet à l'autre — les défauts curated
  de la démo écrasaient les réglages des vrais projets.
- **Fix** (commits 2cba057 + fde5a49) : snapshot par slug (params + unité
  d'expression + counts/rotations), borné à 20, persisté en sessionStorage
  (survit au F5, durée de vie = onglet) — sauvegardé en quittant,
  restauré en revenant ; défauts d'usine pour un premier visit ;
  `pendingCuratedDefaults` pour que les curated defaults démo ne soient
  pas écrasés au premier chargement (au retour, le snapshot gagne).
- **La vue live et les calculs survivaient déjà** (registre global
  2026-08-28 + watch immediate) — pas besoin de locker la navigation.
- **Validé en prod** (compte QA) : projet réglé 1250×2500/11-77 →
  navigation SPA démo (ses propres défauts, zéro fuite) → retour projet =
  tout restauré ; F5 complet = tout restauré (sessionStorage).
- **Écart connu** : réglages non persistés entre onglets/session (c'est un
  brouillon de travail, pas une donnée) ; pas de test unitaire dédié
  (files.js dépend des auto-imports Nuxt — la validation est e2e).
