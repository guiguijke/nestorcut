# Audit 2026-08-31 — moteur de nesting (Rust + Python) et UI

Passe d'audit frais et complet suite aux modifications récentes (pass structurel
grille canonique, isolation live par projet, correctifs QA prod §M, retouches
55632c7). Trois audits en profondeur menés en parallèle :

- **Rust** : `nest-engine` + `sparrow` + `jagua-rs` (~19 000 lignes, lecture
  intégrale), passes sur `workers/geometry/crates/*`.
- **Python** : `workers/nesting/core/*` (~4 200 lignes, lecture intégrale),
  `worker_common`, miroirs JS consultés pour comparaison.
- **UI** : `app/composables`, `app/components`, `app/pages`, workers navigateur,
  côté serveur ce qui touche l'UX nesting (`nest.post.js`, rate limiter,
  entitlements).

Méthode : lecture intégrale des fichiers prioritaires, remontée des appelants
pour confirmer la joignibilité, contre-vérification manuelle des maths
(R±90°, inflation jagua, shoelace), reproduction CLI d'une panique moteur, et
**contre-vérification indépendante de chaque constat critique/majeur par une
seconde lecture du code** après remise des rapports. Les correctifs §A-O de
`AUDIT-2026-08-29.md` n'ont pas été re-signalés ; ils ont été re-vérifiés
comme en place (cf. §U).

**Aucun fichier de code n'a été modifié — audit uniquement.**

Plan d’implémentation moteur (§P / §Q, hors §R déjà livré en §W) :
`docs/archive/2026-08-audits/PLAN-P-Q-moteur.md`.

---

## Santé globale (validation empirique, 2026-08-31)

| Suite / verrou | Résultat |
|---|---|
| `npx vitest run` | **330/330** (29 fichiers) |
| pytest `workers/nesting/tests/` (venv `.qa-pw`) | **104 passés** |
| `cargo test --release -p nest-engine` | **66 passés + 1 ignoré** (socle documenté tenu) |
| `cargo test -p nest-engine` **debug** | 57 passés, **3 échoués** (voir Q.m-1 — release seule est la référence) |
| `determinism_lock.py` natif vs wasm | **OK — bit-identical** (SHA-256 `6c816046…`, tolérance 0) |
| Fraîcheur bundle wasm | `nest_wasm_bg.wasm` rebuildé **dans la même PR** que `gravity.rs` (55632c7, 1 288 831 → 1 500 048 octets) — piège #33b respecté |
| `cache:'reload'` (§L.2) | présent, `public/workers/engine.worker.js:24` |

Lecture : les bugs ci-dessous vivent tous dans des zones **non couvertes par
les tests actuels** (rotationCount ≠ 4, grille −Y débordante, pièces
quasi ajustées, états de file du registre, F5 sans navigation préalable).

---

## §P — Python : pass structurel & finalisation

### P-1 — CRITIQUE : le lattice ignore les rotations permises — pièces livrées dans des orientations interdites (miroir Python + JS)

- **Fichiers** : `workers/nesting/core/structure.py:135` (détection),
  `:401-410` (`small_lattice`), `:590` (`deg0 / deg0 + 180`),
  `:535` (`_lattice_rotated` : `deg + 90`) ; `plan_lattice` pose le rect à
  rotation `0.0` sans validation (`:230`). Miroirs :
  `app/composables/structureClient.js:449-451` (même filtre) et `:338`
  (`attachRings` : `deg0 : deg0 + 180`).
- **Problème** : la détection vérifie que les rotations demandées SONT des
  quarts de tour, mais jamais que les rotations **posées** par le lattice
  (0/180 en zigzag, +90 en variante tournée) sont **permises**.
- **Scénario** : `rotationCount=1` → l'UI sert `rotations=[0]`
  (`nest.post.js:60-64`). Détection acceptée (`[0] ⊂ {0,90,180,270}`), le
  sous-solve moteur respecte `allowed_orientations=[0]`, mais le lattice
  (posé en premier, tout-ou-rien, `structure.py:742-750`) place la moitié des
  fillers à **180°** — pièce asymétrique retournée. L'alternative grille est
  rang 0 (`grid: -1`, `main.py:1485`) : c'est la vue par défaut du modal.
- **Impact** : non-conformité silencieuse du livrable de découpe (fil du
  matériau, sens décoré) sur le cas d'usage produit phare, **des deux côtés**
  (serveur ET navigateur).
- **Fix** : passer les rotations permises à `small_lattice` et ne générer que
  les variantes dont toutes les rotations posées sont permises ; exiger
  `0.0 ∈ rect["rotations"]` dans `detect_structural_case` sinon repli moteur.
  Miroir JS identique.

### P-2 — MAJEUR : `plan_lattice` ne vérifie pas que la grille tient dans la tôle — pièces hors tôle livrées rang 0

- **Fichiers** : `workers/nesting/core/structure.py:218-233` (`cols =
  n_full + (1 if remainder else 0)`, aucune borne `cols * pitch_x` vs
  `sheet_w` ; idem `lattice_top` en objectif y vs `sheet_h`).
- **Preuve** (vérifié calculatoirement) : 310 lattes 510×10 + 100 carrés
  8×8, tôle 1000×2000, space 1 → `per_col=181`, `cols=2`, colonne 2 à
  x ∈ [512, 1022] → **129 pièces à 23 mm hors tôle**. La zone B devient un
  rectangle inversé (zw < 0 → silencieusement vide, `_zone_solve:287`), la
  garde `STRUCT_TOL` (`main.py:1200` : struct_w ≤ best_w × 1,2) laisse
  passer, `verify_layout` pose `insideSheet=False` (`metrics.py:361`) mais
  ce n'est qu'un badge — l'alternative est exportée et triée en tête.
- **Fix** : `return None` si `cols * pitch_x + space > sheet_w` (resp.
  lignes vs `sheet_h` en −Y) ; complément P-4.

### P-3 — MAJEUR : bbox de mesure de la zone B′ transposée — rotation +90° appliquée à une instance transposée à −90° (miroir Python + JS)

- **Fichiers** : `structure.py:729-730`
  (`bbox=(-sy1, sx0, -sy0, sx1)` = bbox de R(+90°)) alors que l'instance de
  solve est `T(-90)·coords` (`main.py:1133` : `[[y, -x] …]`) — la bonne bbox
  est `(sy0, -sx1, sy1, -sx0)`. Miroirs : `structureClient.js:793-796`
  (`transposedBbox`) vs `localJobPrivate.js:125` (même signe faux).
- **Conséquence** : le check anti-débordement `used_w <= solve_w + 1e-3`
  (`structure.py:303`) mesure un bord imaginaire ; écart = 2×ordonnée du
  centroïde d'origine (33,6 mm sur le fan de test à rot 0). En job −Y, la
  bande du haut peut réellement déborder la tôle après map-back — le garde
  censé l'attraper regarde ailleurs. Le map-back lui-même est correct
  (vérifié : `M(G) = R(r)·coords + T(+90)·t + c`).
- **Fix** : bbox de R(−90°) des deux côtés ; ajouter un test qui dérive la
  bbox comme `main.py` et vérifie le rejet d'un débordement réel (le test
  existant `test_zone_solve_transposed_map_back` passe une bbox ad hoc et ne
  verrouille pas cette cohérence).

### P-4 — MAJEUR : aucun garde bloquant sur `insideSheet` à la finalisation

- **Fichiers** : `main.py:1386-1400` (`_finalize_alternative` ne rejette que
  `n != total_requested_count`) ; `metrics.py:361-362` (badge non bloquant).
- **Lecture** : les gardes du pass protègent le NOMBRE de pièces, jamais
  leur position. `verify_layout` calcule déjà le bon verdict (`sheet.covers`)
  mais il n'entre dans aucune décision — P-2/P-3 franchissent donc toute la
  chaîne jusqu'au DXF rang 0.
- **Fix (3 lignes)** : dans `_finalize_alternative`, repli moteur si
  `verification["insideSheet"]` est faux pour une alternative `structural`.
  C'est le filet commun qui transforme chaque futur bug géométrique du pass
  en repli propre. **Priorité n°1 recommandée.**

### P-m — Mineurs Python

1. **`rotations: []` interprété 3 façons** : `main.py:471` (`or [0.0]`),
   `main.py:561` (`[]` passé tel quel au moteur), `main.py:1096` /
   `structure.py:134` (`or [0,90,180,270]`), le JS rejette
   (`structureClient.js:450`). Le serveur `JSON.parse` sans valider
   (`nest.post.js:103`). Fix : normaliser `[]` → `[0.0]` à l'entrée +
   rejet à l'enqueue.
2. **Validation gauche de `used_w` inopérante** (`structure.py:302`) : le
   débordement gauche est comparé à `solve_w` au lieu de ≤ 0 — une pièce qui
   mord la zone A voisine passe. Fix : condition séparée
   `tx + bx0 >= -1e-3`.
3. **`LATTICE_SIMPLIFY_MM` codé dur 0,05** (`structure.py:75`) alors que
   `NEST_SIMPLIFY_MM` est configurable (`main.py:62`) — un opérateur montant
   la simplification à 0,2 obtient un lattice calibré pour 0,05 →
   chevauchements réels à l'export (courbes brutes). Fix : marge
   `space + 2 × SIMPLIFY_MM` depuis la même source.
4. **Budget temps du pass non borné globalement** : zones × tronçons ×
   5 tentatives × 25-45 s → pire cas théorique ≈ 30 min hors budget job
   (magnitude multi-tronçons non documentée au §F.4). Fix : deadline mur
   passé à `build_structural_layout`.
5. **Rétention mémoire des caches DXF** : `dxf_document_cache` vidé au début
   du job SUIVANT (`main.py:349/397`) — le doc ezdxf du job N−1 reste en RAM
   pendant tout l'idle worker. Fix : vider en `finally` de `nesting_process`.
6. **`_shoelace` inexact sur anneau ouvert** (`structure.py:79-83`, segment
   de fermeture manquant) — le miroir JS `polygonArea`
   (`localJobPrivate.js:142-150`) gère la fermeture circulaire. Bénin
   (détection/caps légèrement décalés).
7. **`count` non-entier non validé** (`nest.post.js:56/103` — pas de
   `Number.isInteger`, contrairement aux sheets :160) → erreur de
   désérialisation Rust peu actionnable. Fix : `Math.floor` + rejet < 1.
8. **Fenêtre d'annulation non protégée pendant la finalisation** : aucun
   `should_cancel` entre la fin du solve/du pass et l'écriture finale
   (`main.py:1034→fin`) — une annulation pendant reveal/build (~2-15 s)
   finalise en `done` sans refund. Fix : re-check avant l'écriture.

### P-i — Infos Python

- Garde morte dans `_zone_steps` (`structure.py:676-678`) : condition
  invérifiable pour n ≥ 2 — code mort sans effet (miroir JS idem).
- Contact exact incohérent : `_violates_spacing` (`holefill.py:53`) rejette
  distance == space, `_violates_spacing_loose` (:92) l'accepte —
  conservateur, capacité pinwheel sous-estimée vs docstring.
- Vue live serveur figée pendant le pass (`live_events: False`,
  `main.py:1147`) — pendant plusieurs minutes seule l'étape heartbeat
  s'anime ; les étapes zones step/zone n'existent que côté JS.
- Race heartbeat/$unset (~2 s, `main.py:666-695` vs `1015-1032`) : champ
  `progress` résiduel possible sur un doc error/cancelled — inoffensif (UI
  keyée sur status).
- Perf `generate` (`structure.py:570-591`) : boucle j jusqu'à 220 sans
  break vertical — secondes max, perfectible.

---

## §Q — Rust : moteur (nest-engine / sparrow / jagua-rs)

### Q-1 — MAJEUR : pre-check de faisabilité trop laxiste d'un espacement complet → panique SPP (reproduite)

- **Fichiers** : `workers/nesting/core/main.py:479` + miroir
  `app/composables/localPayloadBuilder.js:577` (les gardes) ; panique dans
  `sparrow/src/optimizer/lbf.rs:74`.
- **Preuve** : les gardes comptent `w + space <= sw` alors que jagua inflate
  `space/2` **de chaque côté ET sur le conteneur**
  (`jagua-rs/src/io/import.rs:37`) — la condition réelle est
  `w + 2·space <= sw`. Reproduction CLI (vérifiée) : strip 10, pièce 8×8,
  `min_item_separation: 2` → le garde passe (8+2≤10), jagua exige 12 →
  élargissement ×1,2 en boucle →
  `panicked at lbf.rs:74: strip-width is running away`.
- **Scénario produit** : pièce quasi ajustée (998 mm sur tôle 1000, space 2)
  — le cas « demande exacte » est un scénario assumé. En SPP,
  `panic = "abort"` (Cargo.toml:28) tue le worker natif ; le Web Worker
  navigateur meurt sur la même panique. Les deux chemins d'erreur aval sont
  propres (natif : `engine.py:160` → EngineError avec stderr ; wasm : pool
  tolérant), donc le coût est un **crash déterministe avec message Rust
  brut** au lieu de « Part(s) too large for the sheet ». En BPP la même
  fenêtre dégrade proprement (`unplaced > 0` → merge → infeasible) — seul
  le SPP panique.
- **Fix** : `w + 2*space <= sw + 1e-6 && h + 2*space <= sh + 1e-6` dans les
  deux gardes ; en durcissement, convertir l'assert `lbf.rs:74` en erreur
  remontée comme événement.

### Q-m — Mineurs Rust

1. **Suite debug rouge (3 échecs)** : `debug_assert!` du quadtree
   (`jagua-rs/src/collision_detection/quadtree/qt_hazard.rs:111`) violé
   quand les arêtes d'un hazard partiel sont exactement alignées sur les
   lignes de quadrant — précisément le métier de ce produit (grilles
   parfaites). En release le repli centroïde classe correctement : pas de
   bug de production, mais l'invariant formel est invisible. + test plateau
   (`constructive.rs:491`) dont la borne ~10 s est fausse en debug
   (19,1 s mesuré). Fix : assouplir le debug_assert (cas aligné) ou
   désaligner les fixtures ; borne plateau release-only.
2. **`separator_workers: 0` non clampé** (`config.rs:191-193` `unwrap_or`
   sans borne basse) → `unwrap()` panique dans `move_items_multi`
   (`separator.rs:176-179`). Chemin mort aujourd'hui (serveur passe None/1,
   pool JS force 1). Fix : `.max(1)`.
3. **Quantités par fichier non bornées hors démo**
   (`nest.post.js:111` — `count: requestFile?.count || 0` sans plafond,
   contrairement au chemin démo `DEMO_MAX_PARTS:125` et aux sheets :160) →
   `PairMatrix` en `size*(size+1)/2` (`tracker.rs:50`,
   `pair_matrix.rs:15-18`) → OOM sur `count=1e9` (accident plus
   qu'attaque, chemin authentifié facturé). Fix : `Number.isInteger` +
   plafond total (ex. 5 000-10 000).
4. **`nest-report` fragile** : `partial_cmp().unwrap()` sur coordonnées
   (`workers/geometry/crates/nest-report/src/lib.rs:104,250,252,274` — NaN
   impossible via JSON mais total_cmp serait gratuit) ; anneaux vides →
   sous-indice usize (`:431`, `:452`, `:528`). Joignibilité improbable
   (l'amont filtre). Fix : `total_cmp` + `if ring.len() < 3 { return }`.

### Q-i — Infos Rust

- `transpose_instance` ignore les `ExtShape::Rectangle` (`spp.rs:176-181`)
  — chemin mort (les builders n'émettent que `simple_polygon`), piège latent
  → suggérer un `bail!` explicite.
- Champ `bin` des frames live BPP = index d'itération, pas `container_id`
  (`bpp/mod.rs:62`) — cohérent avec l'export et purement display ; à
  re-documenter si multi-format de tôles un jour.
- `assert!(loss > 0.0)` actifs en production (`tracker.rs:103,108`) —
  garantis par construction aujourd'hui, fragiles si la quantification
  évolue.
- Config moteur non bornée (`explore_ratio: 1e38` → `from_secs_f32`
  panique, `config.rs:187-189`) — joignabilité quasi nulle (config
  construite serveur/JS). Clamp en durcissement.
- `balance_lane_tops` (`column_fill.rs:1815-1818`) : indices bornés par le
  `ncols` initial — re-détection incompatible impossible tant que
  `detect_uniform_grid` réussit (théorique).
- Feature `simd` de sparrow désactivée partout : si elle est activée un jour
  sur une seule cible, l'ordre de sommation cassera `determinism_lock` —
  à garder dans le runbook.

---

## §R — UI : orchestration locale, état, UX

### R-1 — CRITIQUE : quota free consommé SANS refund sur les échecs de validation postérieurs à la consommation

- **Fichiers** : `server/api/project/[slug]/nest.post.js:297` (charge
  `assertCanNest`) → `:303` (`validateDirections` 403) / `:366-378`
  (409 `concurrent_limit`) / `vault_locked` levé dans
  `enqueueNestingJob` — `server/utils/entitlement.js:452-460`.
- **Preuve** (vérifiée) : `$inc: { freeNestingUsed: 1 }` atomique dès :297 ;
  le 409 `concurrent_limit` (compte pending/processing/awaiting_local) est
  jeté après, sans compensation — le refund n'existe que via le champ
  `charge` du doc job, or aucun job n'est créé. Le commentaire
  « Consumes a unit only once the request is fully validated »
  (`entitlement.js`) est contredit par l'ordre d'exécution.
- **Scénario** : utilisateur Free (1 job parallèle), reclique « Imbriquer »
  pendant qu'un calcul tourne ou double-clique (cf. R-2) → 409 → **1 unité
  gratuite sur 10 perdue, sans job, sans message, sans refund**. Un vault
  verrouillé brûle aussi une unité.
- **Fix** : déplacer `assertCanNest` après TOUTES les validations, ou
  décrémenter `freeNestingUsed` en compensation sur ces trois sites quand
  `charge.type === 'free'`.

### R-2 — MAJEUR : « Imbriquer » sans garde de double soumission, erreurs avalées, bouton désactivé sur échec

- **Fichiers** : `app/composables/files.js:453-511`.
- **Preuve** : pas de verrou in-flight pendant le POST ; le `catch` interne
  ne `return` pas pour les erreurs non listées (409, 500…) → l'exécution
  continue vers `state.lastParams = state.requestBody` (:507) alors que le
  POST a échoué.
- **Scénario** : (a) double-clic pendant la latence → 2 requêtes peuvent
  passer le compteur de concurrence → 2 jobs + 2 unités chez un Free, ou
  1 job + R-1 ; (b) 409/500 → rien à l'écran ET bouton grisé
  (`isNewParams` false) — le « bouton muet » dans une variante distincte de
  §M.3.
- **Fix** : désactivation pendant le POST (ref in-flight), `return`
  explicite dans le catch + message i18n (`nest.error.concurrent`).

### R-3 — MAJEUR : annulation d'un job local EN FILE = zombie + bandeau d'erreur mensonger

- **Fichiers** : `app/components/UserResultItem.vue:144-158` +
  `app/composables/localSolverRegistry.js:87-94, 138-161`.
- **Preuve** : le `cancelJob` du registre (retire de `state.queue`, marque
  `cancelled`) n'est câblé **nulle part dans l'UI** (seuls les tests).
  `cancelNesting` fait POST /cancel + `cancelPool(slug)` — or `cancelPool`
  ne voit que les pools lancés (`pools` Map) ; un job en file survit, et
  `pump()` ne filtre que `phase === 'cancelled'`.
- **Scénario** : Free, job A courant, job B en file ; annulation de B
  (serveur `cancelled` + refund) → A finit → `pump()` lance B →
  `local-payload` répond 409 (garde `awaiting_local`,
  `local-payload.get.js:38`) → registre passe B en `error` → bandeau
  « Le calcul local a échoué… réessayez » sur un job annulé par
  l'utilisateur ; spinner sur un job mort en attendant.
- **Fix** : câbler `localSolverRegistry.cancelJob` dans `cancelNesting`
  (il fait déjà file + pools), et/ou purger dans `pump()` les entrées dont
  le job serveur n'est plus `awaiting_local`.

### R-4 — MAJEUR : pool multi-walks — impasse définitive si le worker de merge meurt

- **Fichiers** : `app/composables/localPool.js:596-673` (`failSlot` +
  `checkAllSettled`).
- **Preuve** : `checkAllSettled` poste l'op `merge` à `survivors[0]` sans
  vérifier que son worker est vivant ni poser de timeout. Si ce worker
  crashe après son solve (`onerror` → `failSlot` : `slot.outcome` reste
  `ok:true`, worker `terminate()`), `failSlot` rappelle `checkAllSettled`
  qui re-poste le merge **au même worker mort** — `postMessage` sur un
  worker terminé est silencieusement droppé → `pool.settle` jamais appelé →
  promesse de `runPool` jamais résolue → `state.running` jamais décrémenté.
- **Scénario** : démo ×4/×8, trap wasm/OOM du worker 0 après un solve
  réussi → carte « Calcul en cours… » à l'infini, et chez un Free le slot
  unique du registre reste occupé à vie.
- **Fix** : timeout de garde sur l'attente du merge qui settle sur les
  survivants ; choisir un worker vivant ET `outcome` ok.

### R-5 — MAJEUR : registre — re-file d'un job en erreur → double run du même slug

- **Fichiers** : `app/composables/localSolverRegistry.js:51-70, 138-150`.
- **Preuve** : `entry()` réutilise l'entrée existante sans remettre `phase`
  à `queued` — un job re-filé après échec (phase `error`) reste hors du
  garde `[queued,running,done]` de `ensureJob` → un deuxième `ensureJob` le
  re-push → 2 entrées en file → `launch()` (qui ne refuse que `cancelled`)
  l'exécute deux fois en concurrence : 2× `local-payload`, 2× `runPool`
  sur le même slug (`pools.set` écrase le premier pool : terminaison et
  décompte faussés).
- **Scénario** : échec réseau du fetch `local-payload` (job serveur reste
  `awaiting_local`, phase registre `error`) → sortie/retour sur la page
  pendant qu'un autre job occupe le slot → duplicata en file. Effet
  additionnel : `phase` affichée reste `error` → pas de libellé « en attente
  d'un slot ».
- **Fix** : réinitialiser `phase='queued'` + vider `result/frame/error` à
  la re-file d'un job `error`.

### R-6 — MAJEUR : vue live locale re-figée — la couche registre casse la fenêtre d'égalité du champion (régression du fix B.4)

- **Fichiers** : `app/composables/localSolverRegistry.js:73-85`
  (`liveFrameBetter`) vs `app/components/LiveNestingView.vue:245-287`
  (`isBetter`).
- **Preuve** (vérifiée) : `liveFrameBetter` compare `strip_width` en
  **égalité stricte**, puis `used_height`, puis densité — sans la fenêtre
  1 mm du corridor phase 2 (`SPP_TIE_WINDOW_MM`), sans `remnant`/`bins`,
  et retourne `false` à égalité parfaite. Or la vue ne reçoit que `j.frame`
  déjà filtré (`[slug].vue:287`) : toute la sémantique du fix B.4 (fenêtre
  `phase2_slack_mm`, tie-break `used_height`, `remnant`/`bins` en BPP,
  fraîcheur à égalité BPP) est court-circuitée en amont.
- **Scénario** : (a) SPP local — phase 2 vit à corridor W₀+1 mm → toutes
  ses frames « pas meilleures » → vue figée sur le layout phase 1 jusqu'au
  reveal final ; (b) BPP local (démo = chemin phare) — densité constante
  dès que `bins` converge → « 1 maj et c'est tout » de retour en THIS
  DEVICE.
- **Fix** : répliquer la sémantique `isBetter` dans `liveFrameBetter`, ou
  transmettre toutes les frames à la vue et laisser son champion-lock
  décider.

### R-7 — MAJEUR : snapshots sessionStorage — réglages perdus au F5 sans navigation inter-projet préalable

- **Fichiers** : `app/composables/files.js:53-91, 256-306`.
- **Preuve** (vérifiée) : `persistSnapshotsToStorage` n'est appelé que par
  `snapshotCurrentProject` (:90), lui-même appelé uniquement au
  **changement** de projet dans `setProjectFiles` (:265). Aucun
  `beforeunload`/`pagehide`/watch (grep vérifié).
- **Scénario** : première visite d'un projet → réglages 1250×2500 +
  quantités → **F5 → défauts d'usine**. Variante pire : projet déjà
  snapshoté → nouveaux réglages → F5 → restauré à l'**ancien** snapshot.
  La validation §O (« F5 complet = tout restauré ») ne couvrait que le cas
  navigation-d'abord — contredit la promesse du commit d663ab8.
- **Fix** : persister sur watch (debounce) des params/counts, ou
  `beforeunload` + navigation.

### R-8 — MAJEUR (limite) : fuite mémoire non bornée sur sessions longues — registre + blobUrls

- **Fichiers** : `localSolverRegistry.js:22-29` +
  `app/composables/localHydrate.js:68-75`.
- **Preuve** : (a) `state.jobs` jamais évincé — chaque job `done` conserve
  `result.alternatives` complets (SVG + **contenu DXF texte**, plusieurs
  Mo/job) et `frame` en RAM alors que tout est déjà dans IndexedDB ;
  (b) `blobUrls` indexé par le **texte DXF complet** (clé string de Mo,
  blob jamais révoqué).
- **Impact** : ~10 jobs locaux → dizaines de Mo retenus indéfiniment,
  perceptible sur mobile. Fix : éviction LRU des `done` (scalaires
  uniquement) ; clé `(slug, altId, sheetIndex)` + révocation à la fermeture
  du modal.

### R-m — Mineurs UI

1. **Grille toujours rang 0 côté navigateur** (`localJobPrivate.js:449`)
   alors que le serveur trie par `layoutCount`/`usedSheetShare`
   (`main.py:1487-1497`) → « Option 1 » différente THIS DEVICE vs serveur ;
   la grille peut être en tête jusqu'à STRUCT_TOL (20 %) moins bonne. Fix :
   même tri mesuré côté JS avant hydratation.
2. **Rotations absentes → rejet JS, défaut Python** (rappel de P-m.1) :
   `structureClient.js:449-450` vs `structure.py:134` — pas de grille
   navigateur là où le serveur en produit une. Fix : même repli.
3. **`ringDist` sommets↔arêtes** (`structureClient.js:95-112`) : deux pièces
   concaves (L, U) dont les arêtes se croisent sans proximité de sommet
   passent la validation JS (le Python valide avec STRtree distance exacte)
   → lattice livré avec chevauchements réels en mode navigateur ; le filet
   `geoComputeReport` ne s'affiche que < 250 pièces/tôle. Fix : garde de
   convexité de la petite pièce ou vérification arête↔arête.
4. **`selfContained: !!meta` vs `bool(hole_plan)`**
   (`localJobPrivate.js:243` vs `main.py:1233`/`:1314`) : un `meta` générique
   (packs) sans `hole_plan` → navigateur préserve la silhouette en cascade,
   serveur applique `apply_hole_fill` → deux livrables selon le lieu de
   calcul. Fix : aligner sur `!!holePlan`.
5. **Clé i18n `progress.stage.final` absente des deux langues** (émise par
   `progress.rs:119-125` ET le reveal local `localJobPrivate.js:333`,
   consommée par `LiveNestingView.vue:555-561`) → affiche la chaîne brute
   « final ». Par ailleurs parité EN/FR **parfaite** (493 = 493, vérifiée).
6. **Course `getProject` sur navigation rapide** (`files.js:223-252`, pas
   d'abort/garde d'ordre) : la réponse la plus lente gagne → params
   restaurés appartenant au mauvais projet (les fichiers sont masqués par la
   garde `projectSlug !== apiPath` de `[slug].vue:417`, pas les params).
   Fix : garder le dernier slug demandé ou AbortController.
7. **Divers** : `isValidParams` retourne en fait « est invalide »
   (`files.js:137-148`, nom inversé) ; `exportCsv` révoque l'URL
   immédiatement après `click()` (`ResultModal.vue:694-699` — risque
   d'avortement du téléchargement, `localDownloads` fait un setTimeout 1 s) ;
   chaînes anglaises en dur « Clean offcut » / « Layout option »
   (`ResultModal.vue:739-746`) non localisées ; `window.__structDiag`
   (`localJobPrivate.js:215-217, 444-448`, le 2e write écrase le 1er) —
   même famille que `__champOffers/__champSwaps` (§F.5) ; perf : `viewBox`
   re-parse le `d` de chaque pièce à chaque changement de champion
   (`LiveNestingView.vue:390-484`, coûteux à 900 pièces) et
   `decorateLiveLayout` jusqu'à 400 ms/frame sur le main thread
   (`localBridge.js:119, 458`).

### R-i — Infos UI

- **§M.3 toujours ouvert** : fetch `local-payload` sans timeout
  (`localJobPrivate.js:350`) → registre bloqué si le serveur ne répond
  jamais ; aucun feedback quand le registre refuse une soumission.
- **`ZONE_CANCELLED` référencé sans import** dans `runLocalJobPrivate`
  (`localJobPrivate.js:451` — le destructeur :104 est interne à
  `buildGridAlternative`) : résolu par l'auto-import Nuxt aujourd'hui, mais
  fragile — hors transform Nuxt c'est un `ReferenceError` pile dans le
  chemin d'annulation. Un import explicite coûte une ligne.
- **`isCancelled` de `buildGridAlternative` est mort** : `gridCancelled`
  (`localJobPrivate.js:439`) jamais mis à `true` — l'annulation réelle
  passe par `cancelPool`/`ZONE_CANCELLED`.
- **Sécurité de base saine** : aucun `v-html`/`innerHTML` sur données
  utilisateur ou SVG (seuls les drapeaux statiques du LocaleSwitcher) ;
  slug utilisé uniquement comme clé Map/sessionStorage, jamais injecté en
  HTML ; `engine.worker.js` propre (fetch `cache:'reload'`, cap mémoire
  1 Go post-run, erreurs catchées).
- **Lifecycle sain** : EventSource fermé au unmount + changement de route
  (`UserResults.vue:52-99`), timers `useLocalMode` nettoyés, workers du
  pool terminés dans `settle()`.

---

## §S — Thèmes transverses

1. **Légalité du livrable vs nombre de pièces** (P-1→P-4) : les gardes du
   pass structurel protègent le compte, jamais la légalité (rotations
   permises) ni la position (bords de tôle). Le filet final existe déjà —
   `verify_layout`/`insideSheet` — et n'est pas branché sur une décision.
   **P-4 d'abord (3 lignes), puis P-1/P-2/P-3.**
2. **Trois couches, trois définitions du champion** (R-6) : le pool, la vue
   et le registre ont chacun leur « meilleure frame » ; la couche la plus
   récente (registre) a réintroduit la sémantique que B.4 avait corrigée.
3. **Frontière validation amont ↔ sémantique moteur** (Q-1, Q-m.3, P-m.7) :
   le demi-espace d'inflation jagua et l'absence de plafonds de quantités
   hors démo — deux endroits où « le garde amont » et « la réalité moteur »
   divergent, avec crash ou OOM au bout.
4. **Bugs mirrorés Python↔JS** (P-1, P-2, P-3, Q-1) : la parité
   structureClient/structure est remarquable — y compris dans les bugs.
   Chaque fix se fait symétriquement des deux côtés, avec un test par côté.
5. **État de file du registre** (R-3, R-4, R-5) : la couche la plus jeune de
   l'orchestration locale concentre les états incohérents (annulation non
   câblée, phase non réinitialisée, merge sans garde de survie).

## §T — Plan de correction priorisé (proposition)

1. **P-4** garde `insideSheet` bloquante à la finalisation — filet commun,
   3 lignes, transforme P-2/P-3 et tout futur bug géométrique en repli
   moteur propre.
2. **R-1** refund quota (déplacer la charge après validation OU compenser)
   + **R-2** garde double soumission — argent utilisateur, déclenchable
   trivialement.
3. **P-1** rotations permises du lattice (Python + JS) — conformité du
   livrable phare.
4. **Q-1** garde `2×space` (Python + JS) — panique reproduite sur cas
   « demande exacte ».
5. **P-2/P-3** fixes géométriques mirrorés (borne tôle + bbox R(−90°)).
6. **R-3/R-4/R-5** robustesse registre/pool (annulation câblée, re-file,
   garde merge) ; **R-6** champion du registre aligné sur la vue.
7. **R-7** persistance snapshot F5 ; **R-8** éviction mémoire.
8. Mineurs en accompagnement (P-m.1/3/7, Q-m.1/2/3, R-m.1/4/5 sont les
   plus utiles).

**Tests à ajouter avec les fixes** : lattice avec `rotations=[0]` (les deux
côtés) ; grille −Y dont le sous-solve déborde (bbox dérivée comme
`main.py`) ; lattes ≈ demi-tôle (P-2) ; pièce quasi ajustée 998/1000/2
(garde 2×space, les deux côtés) ; re-file d'un job `error` ; annulation d'un
job en file ; mort du worker de merge ; F5 sans navigation inter-projet.

## §U — Zones saines (pour mémoire d'audit)

- **Correctifs §A-O re-vérifiés en place** : gardes aire enveloppe
  (`is_spp` + garde #2b), `_finalize_cancelled` joint aux deux
  `except EngineCancelled` (annulation propre de bout en bout, tous les
  raises de `main.py` aboutissent à une finalisation propre ou error+
  refund), skips `self_contained`/`structural` cohérents Python↔JS (y
  compris meta-avec-packs et hole_plan=None), garde anti-perte, comptage
  `expand_meta` exact, `cache:'reload'`, budget rate limiter `:project`.
- **Rust** : conventions de repère int→ext homogènes et verrouillées par
  tests sur les 4 chemins (live SPP/BPP, export, map-back phase 2 — math
  revérifiée) ; merge natif/wasm factorisé et testé ; `column_fill.rs`
  défensif (snapshots par passe, caps) ; annulation natif et wasm intactes ;
  `rand` 0.10.2 ; aucun `unwrap` atteignable trouvé au-delà de Q-m.
- **Python** : aucune perte de pièces possible dans le pass structurel
  (vérifié sur tous les chemins, y compris tout-ou-rien zone B) ; pas de
  fuite d'état entre jobs au-delà de la rétention mémoire (P-m.5).
- **UI** : portage miroir `structureClient.js` ↔ `structure.py` d'une
  fidélité remarquable (constantes lattice/zones/grille vérifiées
  identiques) ; `localPayloadBuilder.js` solide (seed BigInt 63 bits,
  gardes) ; isolation live par projet fonctionnelle ; cycle SSE/listeners
  proprement nettoyé ; pas de XSS ; 330 tests verts.

## §V — Non vérifié / limites de la passe

- Suites bench/e2e non rejouées (docker local) — les constats P-2 (« 129
  pièces hors tôle ») et Q-1 (panique) sont vérifiés calculatoirement/CLI
  mais pas sur un job complet.
- `workers/fileprocessing` et `stripfileprocessing` : passe rapide
  uniquement (fixes J.4/J.5 re-vérifiés, rien de nouveau relevé).
- Bugs géométrie différés (INSERT R·S, SPLINE, dxf_writer) : non
  re-détaillés, toujours documentés §J.
- La parité bit-identique du bundle **géométrie** (import/export wasm)
  repose sur les verrous existants, non réexécutés ; seul le bundle
  **moteur** a été verrouillé (`determinism_lock` OK, cf. tableau).

---

## §W — Livraison des correctifs UI (2026-08-31 soir, même jour)

Suite à la demande user (« corrige tous les bugs liés à l'UI, ne touche pas
au moteur de nesting ») : **tous les constats §R sont corrigés**, ainsi que
les points mineurs non liés à la géométrie du pass structurel. Le moteur
(Rust `nest-engine`/`sparrow`/`jagua-rs`, Python `workers/nesting/core`) et
les miroirs géométriques du pass (lattice, `ringDist`, garde `2×space`,
`selfContained`) sont **intacts** — les constats §P/§Q restent ouverts.

| Constat | Fix | Fichiers |
|---|---|---|
| R-1 quota sans refund | `refundNestCharge()` (miroir JS de `refund.py`, idempotent par drapeau sur l'objet charge) + wrap try/catch de tout le post-charge dans la route bin + refund interne d'`enqueueNestingJob` (vault_locked, échec d'insertion) | `server/utils/entitlement.js`, `server/core/project/service.js`, `server/api/project/[slug]/nest.post.js` |
| R-2 double soumission / erreurs avalées | verrou in-flight `nestBusy` (bouton désactivé pendant le POST), `return` explicite sur erreur non gérée + message i18n `nest.error.concurrent`/`nest.error.generic` affiché en page, `lastParams` plus marqué sur échec | `app/composables/files.js`, `app/pages/project/[slug].vue`, `app/utils/i18n.js` |
| R-3 annulation zombie | l'annulation UI passe par `localSolverRegistry.cancelJob` (POST /cancel + cancelPool préfixe + **retrait de la file** + phase cancelled) au lieu du POST+cancelPool manuel | `app/components/UserResultItem.vue` |
| R-4 impasse merge | `postMerge()` : cible = premier survivant **vivant** (drapeau `dead` posé au crash), timeout 30 s → retry sur un autre survivant (max 2 tentatives) → sinon livraison dégradée du premier run survivant (job borné, jamais de slot figé) ; garde `if (data.id) return` contre les réponses merge tardives d'une cible perdue | `app/composables/localPool.js` |
| R-5 double run au re-file | `ensureJob` remet l'entrée d'un job `error` à `queued` (résultat/frame/error vidés) AVANT le push — une seule entrée en file ; `cancelled` reste terminal | `app/composables/localSolverRegistry.js` |
| R-6 vue live re-figée | définition du champion **partagée** `frameFitsSheet`/`frameIsBetter` dans `app/utils/liveJob.js` (fenêtre SPP 1 mm + `used_height`, `bins`/`remnant` BPP, fraîcheur à égalité parfaite BPP) consommée par le registre ET `LiveNestingView` (plus de divergence de sémantique) | `app/utils/liveJob.js`, `app/composables/localSolverRegistry.js`, `app/components/LiveNestingView.vue` |
| R-7 snapshots F5 | persistance débouncée (500 ms) sur les 9 mutateurs de réglages/quantités + `pagehide` en filet — un F5 sans navigation préalable ne perd plus les réglages | `app/composables/files.js` |
| R-8 fuite mémoire | éviction des payloads lourds du registre 120 s après la fin d'un job (result/frame/itemMap/zone → null, scalaires conservés ; le record complet vit dans IndexedDB) + cache blobUrls DXF borné LRU 24 avec révocation | `app/composables/localSolverRegistry.js`, `app/composables/localHydrate.js` |
| R-m.1 ordre des alternatives | tri d'affichage = finalisation serveur (grille d'abord, classes canoniques, `layoutCount`/`usedSheetShare`) avec réordonnancement parallèle des containers d'artefacts | `app/composables/localJobPrivate.js` |
| R-m.5 i18n `final` | clé `progress.stage.final` ajoutée aux deux langues | `app/utils/i18n.js` |
| R-m.6 race getProject | seule la réponse de la dernière demande émise est appliquée | `app/composables/files.js` |
| R-m.7 divers | `isValidParams`→`isInvalidParams` (nom honnête) ; `exportCsv` révoque l'URL après 1 s de grâce ; `altTitle` localisé (`result.cleanOffcut`/`result.layoutOption` EN+FR) ; `__structDiag` fusionne au lieu d'écraser (sonde QA conservée) ; import STATIQUE de `ZONE_CANCELLED` (plus d'auto-import fragile dans le chemin d'annulation) ; hook `isCancelled`/`gridCancelled` mort supprimé | `files.js`, `ResultModal.vue`, `i18n.js`, `localJobPrivate.js` |
| §M.3 durcissement | fetch `local-payload` borné 60 s (ofetch `timeout`) — un serveur muet ne fige plus le slot du registre à vie (complète R-5 : le job re-file proprement) | `app/composables/localJobPrivate.js` |

**Non corrigés volontairement** (relèvent du moteur ou de ses miroirs
géométriques, à traiter avec §P/§Q) : R-m.2 (défaut de rotations de la
détection — se recouvre avec P-1), R-m.3 (`ringDist` pièces concaves),
R-m.4 (`selfContained: !!meta` vs `hole_plan`), R-m.7 perf (`viewBox`
re-parse, `decorateLiveLayout` main thread) — optimisations, pas des bugs.

**Validation** : `npx vitest run` → **348/348** (30 fichiers ; +18 par
rapport au socle : 8 champion partagé, 3 registre re-file/champion, 4 merge
R-4, 3 refund R-1). `npx nuxt build` vert (composants recompilés).
`window.__champOffers/__champSwaps` conservés (instrumentation QA
documentée §F.5, la sonde `__structDiag` aussi).

---

## §X — Livraison des correctifs moteur §P/§Q (2026-08-31, plan PLAN-P-Q-moteur.md)

Lots 1–4 du plan (§2) implémentés en Python + miroirs JS — **moteur Rust
non touché** (0 fichier `engine/`, pas de rebuild wasm). Lot 5 (optionnel :
`lbf.rs` en erreur, debug cargo, nest-report, deadline mur) **reporté**.

| Constat | Fix | Fichiers |
|---|---|---|
| P-4 filet tôle | `layout_fits_sheet` en fin de `build_structural_layout` (+ garde aval `buildGridAlternative`) ; `_finalize_alternative` jette une alt `structural` avec `insideSheet=false` AVANT l'export DXF (`verify_layout` déplacé avant, un seul appel) ; côté JS, drop miroir après mesure du report wasm. Les alts moteur gardent leur badge (piège #6) | `structure.py`, `structureClient.js`, `main.py`, `localJobPrivate.js` |
| Q-1 garde 2×space | `part_fits_any_sheet` / `partFitsAnySheet` (w + 2·space ≤ sw, jagua inflate space/2 des deux côtés) branché sur le pré-check serveur ET navigateur — la repro 8×8/tôle 10/space 2 (panique SPP `lbf.rs`) devient un message utilisateur | `main.py`, `localPayloadBuilder.js` |
| P-1 rotations lattice | `small_lattice`/`smallLattice` : familles générées seulement si leurs angles posés ∈ rotations permises (zigzag = {deg0, deg0+180}, tourné = {90, 270}) + ceinture dans `consider` (aucune pose illégale ne survit au scoring) ; `detect_structural_case` exige 0° pour la classe rect | `structure.py`, `structureClient.js` |
| P-2 grille ⊆ tôle | `plan_lattice`/`planLattice` : `space + cols·pitch_x ≤ sheet_w` (et rows/pitch_y en −Y) sinon None — les 310 lattes (129 pièces hors tôle) passent en repli moteur | idem |
| P-3 bbox R(−90) | `_transposed_bbox`/`transposedBbox` = bbox des coords (y, −x) — l'instance de solve B′ est T(−90), la mesure aussi ; ≠ `rotatedBbox(90)` (piège #48 intact) | idem |
| P-m.2 overflow gauche | condition séparée `left ≥ −1e-3` dans `_zone_solve`/`zoneSolve` (l'ancien `max(used_w, −(tx+bx0))` comparait le débordement à la LARGEUR de zone) | idem |
| P-m.1 rotations `[]` | normalisées `[0]` à l'entrée (`nest.post.js` parseFileRotations + rejet 400 des angles non finis ; `convert_files_to_input_items`, `allowed_orientations`, `_geom_of`, detect PY+JS ; builder d'instance JS) | `nest.post.js`, `main.py`, `structure.py`, `structureClient.js`, `localPayloadBuilder.js` |
| R-m.4 selfContained | `Boolean(holePlan)` au lieu de `!!meta` (miroir `main.py`) | `localJobPrivate.js` |
| P-m.8 annulation reveal | `should_cancel()` → `_finalize_cancelled()` + `JobCancelled` juste avant la boucle `_finalize_alternative` | `main.py` |
| P-m.7/Q-m.3 counts | `normalizeCount` (entier ≥ 0) + plafond **total** 10 000 pièces/job (400 au-delà) | `nest.post.js` |
| Q-m.2 separator clamp | `max(1, …)` Python + JS (chemin mort Rust non ouvert — pas de cargo/wasm) | `nesting_input_builder.py`, `localPayloadBuilder.js` |
| P-m.3 simplify | `LATTICE_SIMPLIFY_MM = env NEST_SIMPLIFY_MM` (même source que `main.py`) ; JS garde 0,05 avec commentaire de sync | `structure.py`, `structureClient.js` |
| P-m.5 cache DXF | `nesting_process` wrapper try/finally → `dxf_document_cache.clear()` en fin de job (le doc du job N ne survit plus à l'idle du worker) | `main.py` |
| P-m.6 shoelace | boucle circulaire (anneau ouvert exact) PY + JS | `structure.py`, `structureClient.js` |

**Verrous ajoutés** : `workers/nesting/tests/test_feasibility_guard.py`
(T1-T3 + edges, 7 tests) et `test_verrous_audit_2026_08_31.py` (T4, T6-T14,
14 tests) ; miroirs dans `structureClient.test.js` (T4/T6-T9/T14) et
`localPayloadBuilder.test.js` (T1/T2/T14 + table partFitsAnySheet).
Fixtures existantes ajustées : translations gauche-légales dans
`TestZoneSolve`/`TestObjectiveY`/`TestHolePlan` (P-m.2 rend le débordement
gauche bloquant — les anciennes translations naïves débordaient à tort),
rotations explicites sur les 4 fixtures lattice (P-1 : plus de défaut
implicite « toutes poses »), `tx=25` au map-back transposé.

**Validation** : vitest **361/361** (30 fichiers, +13 vs socle 348) ;
pytest nesting **125 passés + 1 sauté** (+21 vs socle 104, venv `.qa-pw`
avec shapely) ; `node --check` + `ast.parse` sur tous les fichiers touchés.
**Veto T10 tenu** : banc phare inchangé (19/colonne, 6 colonnes, reste 5,
PY + JS). Aucun fichier `engine/` ni wasm dans le diff (piège #33b
non applicable).

**Non livrés (lot 5, reportés)** : conversion de l'assert `lbf.rs:74` en
erreur (exigerait rebuild wasm), `debug_assert` quadtree (Q-m.1),
`nest-report` total_cmp (Q-m.4), deadline mur du pass (P-m.4),
`ringDist` arête↔arête (R-m.3, limite JS documentée).

Manuels M1-M7 (navigateur, THIS DEVICE + serveur) : à jouer au
déploiement — le veto numérique est couvert par T10, la validation
visuelle reste à faire.

### §X.2 — Banc Docker local avant publication (2026-08-31 soir)

Build complet des 6 images (compose build) + stack up, app 200, marqueurs
du nouveau code présents dans les images (worker : `part_fits_any_sheet`,
`_transposed_bbox`, `layout_fits_sheet` ; app : `partFitsAnySheet`).
Pytest **dans l'image worker : 124 passés + 2 sautés** (les 3 erreurs
`test_integration_holes` = fileprocessing absent de l'image, préexistantes
et documentées).

Manuels du plan (chemin serveur, `bench/seed_*` + `check_physical.py` —
vérification physique EXHAUSTIVE shapely/STRtree des SVG livrés) :

| # | Scénario | Verdict |
|---|---|---|
| M1 (veto) | 100 Trou + 800 Fillx4, 1000×2000, space 2, −X, 4 rotations | grille 900/900, rotations légales, **0 chevauchement / 0 hors tôle / min-dist 2,0000 exact**, usedSheetShare **73,67 %** (= banc attendu, inchangé) |
| M2 | même job, `rotations=[0]` par fichier | grille **présente** avec **100 % des poses à 0°** (aucun fan tête-bêche — avant P-1, moitié à 180°), physique OK, 900/900 |
| M3 | 1 pièce 998×998, tôle 1000×1000, space 2 | job `error` avec le message exact « Part(s) too large… », **workers vivants, 0 panique** (avant Q-1 : crash SPP `lbf.rs`) |
| M4 | 1 pièce 100×100, tôle 1000×2000, space 2 | `done` 1/1 (contrôle négatif : la garde ne rejette pas ce qui rentre) |

Chemin THIS DEVICE : couvert par les 361 vitest (miroirs
structureClient/localPayloadBuilder, y compris les nouveaux verrous
lattice/bornes/rotations) + build Nuxt — le bundle wasm moteur est
inchangé (0 fichier `engine/`). Scripts de banc posés dans
`workers/nesting/bench/` (`seed_rotation_count1.py`, `seed_near_fit.py`,
`check_physical.py`).
